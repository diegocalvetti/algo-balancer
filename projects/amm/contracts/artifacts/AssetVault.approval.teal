#pragma version 10
intcblock 0 8 1 1000000 4 10 6 5 10000
bytecblock 0x 0x62616c616e6365735f 0x6e756d5f617373657473 0x61737365745f 0x746f6b656e 0x70726f76696465645f 0x151f7c75 0x656e645f726f756e64 0x73746172745f726f756e64 0x776569676874735f 0x0a8101 0x7461726765745f776569676874735f 0x7765696768745f73756d 0x6c6173745f6173736574 0x0000000000000000 0x6d616e61676572 0x6275726e6564

// This TEAL was generated by TEALScript v0.106.2
// https://github.com/algorandfoundation/TEALScript

// This contract is compliant with and/or implements the following ARCs: [ ARC4 ]

// The following ten lines of TEAL handle initial program flow
// This pattern is used to make it easy for anyone to parse the start of the program and determine if a specific action is allowed
// Here, action refers to the OnComplete in combination with whether the app is being created or called
// Every possible action for this contract is represented in the switch statement
// If the action is not implemented in the contract, its respective branch will be "*NOT_IMPLEMENTED" which just contains "err"
txn ApplicationID
!
intc 6 // 6
*
txn OnCompletion
+
switch *call_NoOp *NOT_IMPLEMENTED *NOT_IMPLEMENTED *NOT_IMPLEMENTED *NOT_IMPLEMENTED *NOT_IMPLEMENTED *create_NoOp *NOT_IMPLEMENTED *NOT_IMPLEMENTED *NOT_IMPLEMENTED *NOT_IMPLEMENTED *NOT_IMPLEMENTED

*NOT_IMPLEMENTED:
	// The requested action is not implemented in this contract. Are you using the correct OnComplete? Did you set your app ID?
	err

// createApplication()void
*abi_route_createApplication:
	// execute createApplication()void
	callsub createApplication
	intc 2 // 1
	return

// createApplication(): void
//
// Sets the manager to the creator. Bare create, called once at deployment.
createApplication:
	proto 0 0

	// contracts/AssetVault.algo.ts:61
	// this.manager.value = this.app.creator
	bytec 15 //  "manager"
	txna Applications 0
	app_params_get AppCreator
	pop
	app_global_put

	// contracts/AssetVault.algo.ts:63
	// this.startRound.value = 0
	bytec 8 //  "start_round"
	intc 0 // 0
	app_global_put

	// contracts/AssetVault.algo.ts:64
	// this.endRound.value = 0
	bytec 7 //  "end_round"
	intc 0 // 0
	app_global_put
	retsub

// prepare(address)void
*abi_route_prepare:
	// admin: address
	txna ApplicationArgs 1
	dup
	len
	pushint 32
	==

	// argument 0 (admin) for prepare must be a address
	assert

	// execute prepare(address)void
	callsub prepare
	intc 2 // 1
	return

// prepare(admin: Address): void
//
// Begin bootstrapping: hand the pool over to `admin` and reset asset state.
//
// Called by the Factory (still the manager at this point). The admin then fills
// the pool with one or more {@link addAssets} batches and closes it with
// {@link finalizeBootstrap}. Batching keeps each transaction's resource
// references within the per-group limit, so the pool can hold many assets.
//
// @param admin - account that becomes the pool manager.
prepare:
	proto 1 0

	// contracts/AssetVault.algo.ts:78
	// this.assertIsManager()
	callsub assertIsManager

	// contracts/AssetVault.algo.ts:80
	// this.manager.value = admin
	bytec 15 //  "manager"
	frame_dig -1 // admin: Address
	app_global_put

	// contracts/AssetVault.algo.ts:81
	// this.numAssets.value = 0
	bytec 2 //  "num_assets"
	intc 0 // 0
	app_global_put

	// contracts/AssetVault.algo.ts:82
	// this.weightSumAccum.value = 0
	bytec 12 //  "weight_sum"
	intc 0 // 0
	app_global_put

	// contracts/AssetVault.algo.ts:83
	// this.lastAsset.value = AssetID.zeroIndex
	bytec 13 //  "last_asset"
	intc 0 // 0
	app_global_put

	// contracts/AssetVault.algo.ts:84
	// this.burned.value = 0
	bytec 16 //  "burned"
	intc 0 // 0
	app_global_put
	retsub

// addAssets(uint64[],uint64[])void
*abi_route_addAssets:
	// weights: uint64[]
	txna ApplicationArgs 2
	extract 2 0

	// assets: uint64[]
	txna ApplicationArgs 1
	extract 2 0

	// execute addAssets(uint64[],uint64[])void
	callsub addAssets
	intc 2 // 1
	return

// addAssets(assets: AssetID[], weights: uint64[]): void
//
// Append a batch of assets and weights during bootstrap. Manager-only, and only
// before {@link finalizeBootstrap}. May be called repeatedly; AssetIDs must be
// strictly increasing across all batches. Weights accumulate and are validated
// at finalize.
//
// @param assets  - ASA IDs for this batch (strictly increasing).
// @param weights - Weight per asset, each >= MIN_WEIGHT.
addAssets:
	proto 2 0

	// Push empty bytes after the frame pointer to reserve space for local variables
	bytec 0 // 0x
	dupn 2

	// contracts/AssetVault.algo.ts:97
	// this.assertIsManager()
	callsub assertIsManager

	// contracts/AssetVault.algo.ts:98
	// assert(this.token.value === AssetID.zeroIndex, 'pool already bootstrapped')
	bytec 4 //  "token"
	app_global_get
	intc 0 // 0
	==

	// pool already bootstrapped
	assert

	// contracts/AssetVault.algo.ts:99
	// assert(assets.length === weights.length, 'assets and weights length mismatch')
	frame_dig -1 // assets: AssetID[]
	len
	intc 1 // 8
	/
	frame_dig -2 // weights: uint64[]
	len
	intc 1 // 8
	/
	==

	// assets and weights length mismatch
	assert

	// contracts/AssetVault.algo.ts:102
	// for (let b = 0; b < assets.length / 4 + 1; b += 1)
	intc 0 // 0
	frame_bury 0 // b: uint64

*for_0:
	// contracts/AssetVault.algo.ts:102
	// b < assets.length / 4 + 1
	frame_dig 0 // b: uint64
	frame_dig -1 // assets: AssetID[]
	len
	intc 1 // 8
	/
	intc 4 // 4
	/
	intc 2 // 1
	+
	<
	bz *for_0_end

	// contracts/AssetVault.algo.ts:103
	// increaseOpcodeBudget()
	itxn_begin
	intc 6 //  appl
	itxn_field TypeEnum
	intc 0 // 0
	itxn_field Fee
	bytec 10 //  #pragma version 10; int 1
	dup
	itxn_field ApprovalProgram
	itxn_field ClearStateProgram
	intc 7 //  DeleteApplication
	itxn_field OnCompletion
	itxn_submit

*for_0_continue:
	// contracts/AssetVault.algo.ts:102
	// b += 1
	frame_dig 0 // b: uint64
	intc 2 // 1
	+
	frame_bury 0 // b: uint64
	b *for_0

*for_0_end:
	// contracts/AssetVault.algo.ts:106
	// for (let i = 0; i < assets.length; i += 1)
	intc 0 // 0
	frame_bury 1 // i: uint64

*for_1:
	// contracts/AssetVault.algo.ts:106
	// i < assets.length
	frame_dig 1 // i: uint64
	frame_dig -1 // assets: AssetID[]
	len
	intc 1 // 8
	/
	<
	bz *for_1_end

	// contracts/AssetVault.algo.ts:107
	// assert(weights[i] >= MIN_WEIGHT, 'weight too small')
	frame_dig -2 // weights: uint64[]
	store 255 // full array
	intc 0 //  initial offset
	frame_dig 1 // i: uint64
	intc 1 // 8
	* // acc * typeLength
	+
	load 255 // full array
	swap
	intc 1 // 8
	extract3
	btoi
	intc 8 // 10000
	>=

	// weight too small
	assert

	// contracts/AssetVault.algo.ts:108
	// assert(assets[i] > this.lastAsset.value, 'assets must be strictly increasing')
	frame_dig -1 // assets: AssetID[]
	store 255 // full array
	intc 0 //  initial offset
	frame_dig 1 // i: uint64
	intc 1 // 8
	* // acc * typeLength
	+
	load 255 // full array
	swap
	intc 1 // 8
	extract3
	btoi
	bytec 13 //  "last_asset"
	app_global_get
	>

	// assets must be strictly increasing
	assert

	// contracts/AssetVault.algo.ts:110
	// index = this.numAssets.value
	bytec 2 //  "num_assets"
	app_global_get
	frame_bury 2 // index: uint64

	// contracts/AssetVault.algo.ts:111
	// this.optIn(assets[i])
	frame_dig -1 // assets: AssetID[]
	store 255 // full array
	intc 0 //  initial offset
	frame_dig 1 // i: uint64
	intc 1 // 8
	* // acc * typeLength
	+
	load 255 // full array
	swap
	intc 1 // 8
	extract3
	btoi
	callsub optIn

	// contracts/AssetVault.algo.ts:112
	// this.addToken(index, assets[i], weights[i])
	frame_dig -2 // weights: uint64[]
	store 255 // full array
	intc 0 //  initial offset
	frame_dig 1 // i: uint64
	intc 1 // 8
	* // acc * typeLength
	+
	load 255 // full array
	swap
	intc 1 // 8
	extract3
	btoi
	frame_dig -1 // assets: AssetID[]
	store 255 // full array
	intc 0 //  initial offset
	frame_dig 1 // i: uint64
	intc 1 // 8
	* // acc * typeLength
	+
	load 255 // full array
	swap
	intc 1 // 8
	extract3
	btoi
	frame_dig 2 // index: uint64
	callsub addToken

	// contracts/AssetVault.algo.ts:114
	// this.weightSumAccum.value = this.weightSumAccum.value + weights[i]
	bytec 12 //  "weight_sum"
	dup
	app_global_get
	frame_dig -2 // weights: uint64[]
	store 255 // full array
	intc 0 //  initial offset
	frame_dig 1 // i: uint64
	intc 1 // 8
	* // acc * typeLength
	+
	load 255 // full array
	swap
	intc 1 // 8
	extract3
	btoi
	+
	app_global_put

	// contracts/AssetVault.algo.ts:115
	// this.lastAsset.value = assets[i]
	bytec 13 //  "last_asset"
	frame_dig -1 // assets: AssetID[]
	store 255 // full array
	intc 0 //  initial offset
	frame_dig 1 // i: uint64
	intc 1 // 8
	* // acc * typeLength
	+
	load 255 // full array
	swap
	intc 1 // 8
	extract3
	btoi
	app_global_put

	// contracts/AssetVault.algo.ts:116
	// this.numAssets.value = index + 1
	bytec 2 //  "num_assets"
	frame_dig 2 // index: uint64
	intc 2 // 1
	+
	app_global_put

*for_1_continue:
	// contracts/AssetVault.algo.ts:106
	// i += 1
	frame_dig 1 // i: uint64
	intc 2 // 1
	+
	frame_bury 1 // i: uint64
	b *for_1

*for_1_end:
	// contracts/AssetVault.algo.ts:119
	// assert(this.numAssets.value <= SCALE / MIN_WEIGHT, 'too many tokens')
	bytec 2 //  "num_assets"
	app_global_get
	pushint 100
	<=

	// too many tokens
	assert
	retsub

// finalizeBootstrap()uint64
*abi_route_finalizeBootstrap:
	// The ABI return prefix
	bytec 6 // 0x151f7c75

	// execute finalizeBootstrap()uint64
	callsub finalizeBootstrap
	itob
	concat
	log
	intc 2 // 1
	return

// finalizeBootstrap(): AssetID
//
// Close bootstrapping: require the weights to sum to SCALE and mint the LP token.
// The pool is live (accepts liquidity) afterwards. Manager-only.
//
// @returns the LP token AssetID.
finalizeBootstrap:
	proto 0 1

	// contracts/AssetVault.algo.ts:129
	// this.assertIsManager()
	callsub assertIsManager

	// contracts/AssetVault.algo.ts:130
	// assert(this.token.value === AssetID.zeroIndex, 'pool already bootstrapped')
	bytec 4 //  "token"
	app_global_get
	intc 0 // 0
	==

	// pool already bootstrapped
	assert

	// contracts/AssetVault.algo.ts:131
	// assert(this.numAssets.value >= 1, 'no assets added')
	bytec 2 //  "num_assets"
	app_global_get
	intc 2 // 1
	>=

	// no assets added
	assert

	// contracts/AssetVault.algo.ts:132
	// assert(this.absDiff(this.weightSumAccum.value, SCALE) <= 1, 'weights must sum to 1')
	intc 3 // 1000000
	bytec 12 //  "weight_sum"
	app_global_get
	callsub absDiff
	intc 2 // 1
	<=

	// weights must sum to 1
	assert

	// contracts/AssetVault.algo.ts:134
	// this.createToken()
	callsub createToken

	// contracts/AssetVault.algo.ts:136
	// return this.token.value;
	bytec 4 //  "token"
	app_global_get
	retsub

// addLiquidity(uint64,axfer)void
*abi_route_addLiquidity:
	// txn: axfer
	txn GroupIndex
	intc 2 // 1
	-
	dup
	gtxns TypeEnum
	intc 4 //  axfer
	==

	// argument 0 (txn) for addLiquidity must be a axfer transaction
	assert

	// index: uint64
	txna ApplicationArgs 1
	btoi

	// execute addLiquidity(uint64,axfer)void
	callsub addLiquidity
	intc 2 // 1
	return

// addLiquidity(index: uint64, txn: AssetTransferTxn): void
//
// Deposit one asset toward a liquidity position. The amount is held in `provided`
// until {@link getLiquidity} converts the full position into LP tokens.
//
// @param index - index of the deposited asset in the pool.
// @param txn   - asset transfer carrying the deposit.
addLiquidity:
	proto 2 0

	// Push empty bytes after the frame pointer to reserve space for local variables
	bytec 0 // 0x
	dupn 2

	// contracts/AssetVault.algo.ts:147
	// this.assertIsBootstrapped()
	callsub assertIsBootstrapped

	// contracts/AssetVault.algo.ts:148
	// this.tryFinalizeWeights()
	callsub tryFinalizeWeights

	// contracts/AssetVault.algo.ts:150
	// sender = txn.sender
	frame_dig -2 // txn: AssetTransferTxn
	gtxns Sender
	frame_bury 0 // sender: address

	// contracts/AssetVault.algo.ts:151
	// amount = txn.assetAmount
	frame_dig -2 // txn: AssetTransferTxn
	gtxns AssetAmount
	frame_bury 1 // amount: uint64

	// contracts/AssetVault.algo.ts:153
	// assetId = this.assetAt(index).value
	bytec 3 //  "asset_"
	frame_dig -1 // index: uint64
	itob
	concat
	box_get

	// box value does not exist: this.assetAt(index).value
	assert
	btoi
	frame_bury 2 // assetId: uint64

	// contracts/AssetVault.algo.ts:155
	// assert(txn.xferAsset === assetId, 'wrong asset for this index')
	frame_dig -2 // txn: AssetTransferTxn
	gtxns XferAsset
	frame_dig 2 // assetId: uint64
	==

	// wrong asset for this index
	assert

	// contracts/AssetVault.algo.ts:156
	// assert(txn.assetReceiver === this.app.address, 'deposit must be sent to the pool')
	frame_dig -2 // txn: AssetTransferTxn
	gtxns AssetReceiver
	global CurrentApplicationAddress
	==

	// deposit must be sent to the pool
	assert

	// contracts/AssetVault.algo.ts:158
	// this.optIn(assetId)
	frame_dig 2 // assetId: uint64
	callsub optIn

	// contracts/AssetVault.algo.ts:159
	// this.balances(assetId).value += amount
	bytec 1 //  "balances_"
	frame_dig 2 // assetId: uint64
	itob
	concat
	box_get

	// box value does not exist: this.balances(assetId).value
	assert
	btoi
	frame_dig 1 // amount: uint64
	+
	bytec 1 //  "balances_"
	frame_dig 2 // assetId: uint64
	itob
	concat
	swap
	itob
	box_put

	// *if0_condition
	// contracts/AssetVault.algo.ts:161
	// !this.provided(sender).exists
	bytec 5 //  "provided_"
	frame_dig 0 // sender: address
	concat
	box_len
	swap
	pop
	!
	bz *if0_end

	// *if0_consequent
	// contracts/AssetVault.algo.ts:162
	// this.provided(sender).create((this.numAssets.value + 1) * 8)
	bytec 5 //  "provided_"
	frame_dig 0 // sender: address
	concat
	bytec 2 //  "num_assets"
	app_global_get
	intc 2 // 1
	+
	intc 1 // 8
	*
	box_create
	pop

*if0_end:
	// contracts/AssetVault.algo.ts:165
	// this.provided(sender).value[index] += amount
	bytec 5 //  "provided_"
	frame_dig 0 // sender: address
	concat
	box_get

	// box value does not exist: this.provided(sender).value
	assert
	extract 2 0
	store 255 // full array
	intc 0 //  initial offset
	frame_dig -1 // index: uint64
	intc 1 // 8
	* // acc * typeLength
	+
	load 255 // full array
	swap
	bytec 5 //  "provided_"
	frame_dig 0 // sender: address
	concat
	box_get

	// box value does not exist: this.provided(sender).value
	assert
	extract 2 0
	store 255 // full array
	intc 0 //  initial offset
	frame_dig -1 // index: uint64
	intc 1 // 8
	* // acc * typeLength
	+
	load 255 // full array
	swap
	intc 1 // 8
	extract3
	btoi
	frame_dig 1 // amount: uint64
	+
	itob
	replace3
	bytec 5 //  "provided_"
	frame_dig 0 // sender: address
	concat
	dup
	box_del
	pop
	swap
	dup
	len
	intc 1 // 8
	/
	itob
	extract 6 2
	swap
	concat
	box_put
	retsub

// getLiquidity()uint64
*abi_route_getLiquidity:
	// The ABI return prefix
	bytec 6 // 0x151f7c75

	// execute getLiquidity()uint64
	callsub getLiquidity
	itob
	concat
	log
	intc 2 // 1
	return

// getLiquidity(): uint64
//
// Mint LP tokens for the sender's pending deposits, then clear them.
//
// The first provider receives a fixed amount (AMOUNT_LP_DEPLOYER) and must seed
// every asset. Subsequent providers receive an amount proportional to the value
// they add, via {@link computeNAssetsLiquidity}.
//
// @returns the amount of LP tokens minted.
getLiquidity:
	proto 0 1

	// Push empty bytes after the frame pointer to reserve space for local variables
	bytec 0 // 0x
	dupn 3

	// contracts/AssetVault.algo.ts:178
	// this.assertIsBootstrapped()
	callsub assertIsBootstrapped

	// contracts/AssetVault.algo.ts:179
	// this.tryFinalizeWeights()
	callsub tryFinalizeWeights

	// contracts/AssetVault.algo.ts:181
	// sender = this.txn.sender
	txn Sender
	frame_bury 0 // sender: address

	// contracts/AssetVault.algo.ts:182
	// amount: uint64 = 0
	intc 0 // 0
	frame_bury 1 // amount: uint64

	// *if1_condition
	// contracts/AssetVault.algo.ts:184
	// this.totalLP() === 0
	callsub totalLP
	intc 0 // 0
	==
	bz *if1_else

	// *if1_consequent
	// contracts/AssetVault.algo.ts:187
	// for (let i = 0; i < this.numAssets.value; i += 1)
	intc 0 // 0
	frame_bury 2 // i: uint64

*for_2:
	// contracts/AssetVault.algo.ts:187
	// i < this.numAssets.value
	frame_dig 2 // i: uint64
	bytec 2 //  "num_assets"
	app_global_get
	<
	bz *for_2_end

	// contracts/AssetVault.algo.ts:188
	// assert(this.balances(this.assetAt(i).value).value > 0, 'first deposit must seed every asset')
	bytec 1 //  "balances_"
	bytec 3 //  "asset_"
	frame_dig 2 // i: uint64
	itob
	concat
	box_get

	// box value does not exist: this.assetAt(i).value
	assert
	btoi
	itob
	concat
	box_get

	// box value does not exist: this.balances(this.assetAt(i).value).value
	assert
	btoi
	intc 0 // 0
	>

	// first deposit must seed every asset
	assert

*for_2_continue:
	// contracts/AssetVault.algo.ts:187
	// i += 1
	frame_dig 2 // i: uint64
	intc 2 // 1
	+
	frame_bury 2 // i: uint64
	b *for_2

*for_2_end:
	// contracts/AssetVault.algo.ts:190
	// amount = AMOUNT_LP_DEPLOYER
	pushint 1_000_000
	intc 5 // 10
	intc 6 // 6
	exp
	*
	frame_bury 1 // amount: uint64
	b *if1_end

*if1_else:
	// contracts/AssetVault.algo.ts:192
	// amount = this.computeNAssetsLiquidity(sender)
	frame_dig 0 // sender: address
	callsub computeNAssetsLiquidity
	frame_bury 1 // amount: uint64

*if1_end:
	// contracts/AssetVault.algo.ts:195
	// for (let i = 0; i < this.provided(sender).value.length; i += 1)
	intc 0 // 0
	frame_bury 3 // i: uint64

*for_3:
	// contracts/AssetVault.algo.ts:195
	// i < this.provided(sender).value.length
	frame_dig 3 // i: uint64
	bytec 5 //  "provided_"
	frame_dig 0 // sender: address
	concat
	box_get

	// box value does not exist: this.provided(sender).value
	assert
	extract 2 0
	len
	intc 1 // 8
	/
	<
	bz *for_3_end

	// contracts/AssetVault.algo.ts:196
	// this.provided(sender).value[i] = 0
	bytec 5 //  "provided_"
	frame_dig 0 // sender: address
	concat
	box_get

	// box value does not exist: this.provided(sender).value
	assert
	extract 2 0
	store 255 // full array
	intc 0 //  initial offset
	frame_dig 3 // i: uint64
	intc 1 // 8
	* // acc * typeLength
	+
	load 255 // full array
	swap
	bytec 14 // 0x0000000000000000
	replace3
	bytec 5 //  "provided_"
	frame_dig 0 // sender: address
	concat
	dup
	box_del
	pop
	swap
	dup
	len
	intc 1 // 8
	/
	itob
	extract 6 2
	swap
	concat
	box_put

*for_3_continue:
	// contracts/AssetVault.algo.ts:195
	// i += 1
	frame_dig 3 // i: uint64
	intc 2 // 1
	+
	frame_bury 3 // i: uint64
	b *for_3

*for_3_end:
	// contracts/AssetVault.algo.ts:199
	// sendAssetTransfer({
	//       assetReceiver: sender,
	//       assetAmount: amount,
	//       xferAsset: this.token.value,
	//     })
	itxn_begin
	intc 4 //  axfer
	itxn_field TypeEnum

	// contracts/AssetVault.algo.ts:200
	// assetReceiver: sender
	frame_dig 0 // sender: address
	itxn_field AssetReceiver

	// contracts/AssetVault.algo.ts:201
	// assetAmount: amount
	frame_dig 1 // amount: uint64
	itxn_field AssetAmount

	// contracts/AssetVault.algo.ts:202
	// xferAsset: this.token.value
	bytec 4 //  "token"
	app_global_get
	itxn_field XferAsset

	// Fee field not set, defaulting to 0
	intc 0 // 0
	itxn_field Fee

	// Submit inner transaction
	itxn_submit

	// contracts/AssetVault.algo.ts:205
	// return amount;
	frame_dig 1 // amount: uint64

	// set the subroutine return value
	frame_bury 0

	// pop all local variables from the stack
	popn 3
	retsub

// burnLiquidity(axfer)void
*abi_route_burnLiquidity:
	// transferTxn: axfer
	txn GroupIndex
	intc 2 // 1
	-
	dup
	gtxns TypeEnum
	intc 4 //  axfer
	==

	// argument 0 (transferTxn) for burnLiquidity must be a axfer transaction
	assert

	// execute burnLiquidity(axfer)void
	callsub burnLiquidity
	intc 2 // 1
	return

// burnLiquidity(transferTxn: AssetTransferTxn): void
//
// Burn LP tokens and return the holder's proportional share of every asset.
//
// @param transferTxn - LP transfer into the pool; its amount is the LP burned.
burnLiquidity:
	proto 1 0

	// Push empty bytes after the frame pointer to reserve space for local variables
	bytec 0 // 0x
	dupn 7

	// contracts/AssetVault.algo.ts:214
	// this.assertIsBootstrapped()
	callsub assertIsBootstrapped

	// contracts/AssetVault.algo.ts:215
	// this.tryFinalizeWeights()
	callsub tryFinalizeWeights

	// contracts/AssetVault.algo.ts:217
	// sender = this.txn.sender
	txn Sender
	frame_bury 0 // sender: address

	// contracts/AssetVault.algo.ts:218
	// amountLP = transferTxn.assetAmount
	frame_dig -1 // transferTxn: AssetTransferTxn
	gtxns AssetAmount
	frame_bury 1 // amountLP: uint64

	// contracts/AssetVault.algo.ts:220
	// assert(amountLP > 0, 'Must burn positive amount')
	frame_dig 1 // amountLP: uint64
	intc 0 // 0
	>

	// Must burn positive amount
	assert

	// contracts/AssetVault.algo.ts:221
	// assert(transferTxn.xferAsset === this.token.value, 'must transfer the LP token')
	frame_dig -1 // transferTxn: AssetTransferTxn
	gtxns XferAsset
	bytec 4 //  "token"
	app_global_get
	==

	// must transfer the LP token
	assert

	// contracts/AssetVault.algo.ts:222
	// assert(transferTxn.assetReceiver === this.app.address, 'LP must be sent to the pool')
	frame_dig -1 // transferTxn: AssetTransferTxn
	gtxns AssetReceiver
	global CurrentApplicationAddress
	==

	// LP must be sent to the pool
	assert

	// contracts/AssetVault.algo.ts:226
	// totalLP = this.totalLP() + amountLP
	callsub totalLP
	frame_dig 1 // amountLP: uint64
	+
	frame_bury 2 // totalLP: uint64

	// contracts/AssetVault.algo.ts:227
	// numAssets = this.numAssets.value
	bytec 2 //  "num_assets"
	app_global_get
	frame_bury 3 // numAssets: uint64

	// contracts/AssetVault.algo.ts:229
	// for (let i = 0; i < numAssets; i += 1)
	intc 0 // 0
	frame_bury 4 // i: uint64

*for_4:
	// contracts/AssetVault.algo.ts:229
	// i < numAssets
	frame_dig 4 // i: uint64
	frame_dig 3 // numAssets: uint64
	<
	bz *for_4_end

	// contracts/AssetVault.algo.ts:230
	// assetId = this.assetAt(i).value
	bytec 3 //  "asset_"
	frame_dig 4 // i: uint64
	itob
	concat
	box_get

	// box value does not exist: this.assetAt(i).value
	assert
	btoi
	frame_bury 5 // assetId: uint64

	// contracts/AssetVault.algo.ts:231
	// poolBalance = this.balances(assetId).value
	bytec 1 //  "balances_"
	frame_dig 5 // assetId: uint64
	itob
	concat
	box_get

	// box value does not exist: this.balances(assetId).value
	assert
	btoi
	frame_bury 6 // poolBalance: uint64

	// contracts/AssetVault.algo.ts:233
	// assetAmount = wideRatio([amountLP, poolBalance], [totalLP])
	frame_dig 1 // amountLP: uint64
	frame_dig 6 // poolBalance: uint64
	mulw
	intc 0 // 0
	frame_dig 2 // totalLP: uint64
	divmodw
	pop
	pop
	swap
	!

	// wideRatio failed
	assert
	frame_bury 7 // assetAmount: uint64

	// contracts/AssetVault.algo.ts:235
	// this.balances(assetId).value = poolBalance - assetAmount
	bytec 1 //  "balances_"
	frame_dig 5 // assetId: uint64
	itob
	concat
	frame_dig 6 // poolBalance: uint64
	frame_dig 7 // assetAmount: uint64
	-
	itob
	box_put

	// contracts/AssetVault.algo.ts:237
	// sendAssetTransfer({
	//         assetReceiver: sender,
	//         assetAmount: assetAmount,
	//         xferAsset: assetId,
	//       })
	itxn_begin
	intc 4 //  axfer
	itxn_field TypeEnum

	// contracts/AssetVault.algo.ts:238
	// assetReceiver: sender
	frame_dig 0 // sender: address
	itxn_field AssetReceiver

	// contracts/AssetVault.algo.ts:239
	// assetAmount: assetAmount
	frame_dig 7 // assetAmount: uint64
	itxn_field AssetAmount

	// contracts/AssetVault.algo.ts:240
	// xferAsset: assetId
	frame_dig 5 // assetId: uint64
	itxn_field XferAsset

	// Fee field not set, defaulting to 0
	intc 0 // 0
	itxn_field Fee

	// Submit inner transaction
	itxn_submit

*for_4_continue:
	// contracts/AssetVault.algo.ts:229
	// i += 1
	frame_dig 4 // i: uint64
	intc 2 // 1
	+
	frame_bury 4 // i: uint64
	b *for_4

*for_4_end:
	retsub

// swap(uint64,uint64,uint64,axfer)uint64
*abi_route_swap:
	// The ABI return prefix
	bytec 6 // 0x151f7c75

	// transferTxn: axfer
	txn GroupIndex
	intc 2 // 1
	-
	dup
	gtxns TypeEnum
	intc 4 //  axfer
	==

	// argument 0 (transferTxn) for swap must be a axfer transaction
	assert

	// minAmountOut: uint64
	txna ApplicationArgs 3
	btoi

	// to: uint64
	txna ApplicationArgs 2
	btoi

	// from: uint64
	txna ApplicationArgs 1
	btoi

	// execute swap(uint64,uint64,uint64,axfer)uint64
	callsub swap
	itob
	concat
	log
	intc 2 // 1
	return

// swap(from: uint64, to: uint64, minAmountOut: uint64, transferTxn: AssetTransferTxn): uint64
//
// Swap `from` for `to` along the weighted constant-mean curve.
//
// The input asset is sent into the pool, the output asset back to the sender.
// Reverts if the computed output is below `minAmountOut` (slippage guard).
//
// @param from         - index of the input asset.
// @param to           - index of the output asset.
// @param minAmountOut - minimum acceptable output.
// @param transferTxn  - asset transfer carrying the input amount.
// @returns the output amount sent to the sender.
swap:
	proto 4 1

	// Push empty bytes after the frame pointer to reserve space for local variables
	bytec 0 // 0x
	dupn 8

	// contracts/AssetVault.algo.ts:258
	// this.assertIsBootstrapped()
	callsub assertIsBootstrapped

	// contracts/AssetVault.algo.ts:259
	// this.tryFinalizeWeights()
	callsub tryFinalizeWeights

	// contracts/AssetVault.algo.ts:260
	// increaseOpcodeBudget()
	itxn_begin
	intc 6 //  appl
	itxn_field TypeEnum
	intc 0 // 0
	itxn_field Fee
	bytec 10 //  #pragma version 10; int 1
	dup
	itxn_field ApprovalProgram
	itxn_field ClearStateProgram
	intc 7 //  DeleteApplication
	itxn_field OnCompletion
	itxn_submit

	// contracts/AssetVault.algo.ts:262
	// sender = transferTxn.sender
	frame_dig -4 // transferTxn: AssetTransferTxn
	gtxns Sender
	frame_bury 0 // sender: address

	// contracts/AssetVault.algo.ts:263
	// amount = transferTxn.assetAmount
	frame_dig -4 // transferTxn: AssetTransferTxn
	gtxns AssetAmount
	frame_bury 1 // amount: uint64

	// contracts/AssetVault.algo.ts:265
	// assetIn = this.assetAt(from).value
	bytec 3 //  "asset_"
	frame_dig -1 // from: uint64
	itob
	concat
	box_get

	// box value does not exist: this.assetAt(from).value
	assert
	btoi
	frame_bury 2 // assetIn: uint64

	// contracts/AssetVault.algo.ts:266
	// assetOut = this.assetAt(to).value
	bytec 3 //  "asset_"
	frame_dig -2 // to: uint64
	itob
	concat
	box_get

	// box value does not exist: this.assetAt(to).value
	assert
	btoi
	frame_bury 3 // assetOut: uint64

	// contracts/AssetVault.algo.ts:268
	// assert(transferTxn.xferAsset === assetIn, 'wrong input asset')
	frame_dig -4 // transferTxn: AssetTransferTxn
	gtxns XferAsset
	frame_dig 2 // assetIn: uint64
	==

	// wrong input asset
	assert

	// contracts/AssetVault.algo.ts:269
	// assert(transferTxn.assetReceiver === this.app.address, 'input must be sent to the pool')
	frame_dig -4 // transferTxn: AssetTransferTxn
	gtxns AssetReceiver
	global CurrentApplicationAddress
	==

	// input must be sent to the pool
	assert

	// contracts/AssetVault.algo.ts:271
	// balanceIn = this.balances(assetIn).value
	bytec 1 //  "balances_"
	frame_dig 2 // assetIn: uint64
	itob
	concat
	box_get

	// box value does not exist: this.balances(assetIn).value
	assert
	btoi
	frame_bury 4 // balanceIn: uint64

	// contracts/AssetVault.algo.ts:272
	// balanceOut = this.balances(assetOut).value
	bytec 1 //  "balances_"
	frame_dig 3 // assetOut: uint64
	itob
	concat
	box_get

	// box value does not exist: this.balances(assetOut).value
	assert
	btoi
	frame_bury 5 // balanceOut: uint64

	// contracts/AssetVault.algo.ts:274
	// weightIn = this.getCurrentWeight(from)
	frame_dig -1 // from: uint64
	callsub getCurrentWeight
	frame_bury 6 // weightIn: uint64

	// contracts/AssetVault.algo.ts:275
	// weightOut = this.getCurrentWeight(to)
	frame_dig -2 // to: uint64
	callsub getCurrentWeight
	frame_bury 7 // weightOut: uint64

	// contracts/AssetVault.algo.ts:277
	// amountOut = this.calcOut(balanceIn, weightIn, balanceOut, weightOut, amount)
	frame_dig 1 // amount: uint64
	frame_dig 7 // weightOut: uint64
	frame_dig 5 // balanceOut: uint64
	frame_dig 6 // weightIn: uint64
	frame_dig 4 // balanceIn: uint64
	callsub calcOut
	frame_bury 8 // amountOut: uint64

	// contracts/AssetVault.algo.ts:279
	// assert(amountOut >= minAmountOut, 'Slippage exceeded')
	frame_dig 8 // amountOut: uint64
	frame_dig -3 // minAmountOut: uint64
	>=

	// Slippage exceeded
	assert

	// contracts/AssetVault.algo.ts:281
	// this.balances(assetIn).value = balanceIn + amount
	bytec 1 //  "balances_"
	frame_dig 2 // assetIn: uint64
	itob
	concat
	frame_dig 4 // balanceIn: uint64
	frame_dig 1 // amount: uint64
	+
	itob
	box_put

	// contracts/AssetVault.algo.ts:282
	// this.balances(assetOut).value = balanceOut - amountOut
	bytec 1 //  "balances_"
	frame_dig 3 // assetOut: uint64
	itob
	concat
	frame_dig 5 // balanceOut: uint64
	frame_dig 8 // amountOut: uint64
	-
	itob
	box_put

	// contracts/AssetVault.algo.ts:284
	// sendAssetTransfer({
	//       assetReceiver: sender,
	//       assetAmount: amountOut,
	//       xferAsset: assetOut,
	//     })
	itxn_begin
	intc 4 //  axfer
	itxn_field TypeEnum

	// contracts/AssetVault.algo.ts:285
	// assetReceiver: sender
	frame_dig 0 // sender: address
	itxn_field AssetReceiver

	// contracts/AssetVault.algo.ts:286
	// assetAmount: amountOut
	frame_dig 8 // amountOut: uint64
	itxn_field AssetAmount

	// contracts/AssetVault.algo.ts:287
	// xferAsset: assetOut
	frame_dig 3 // assetOut: uint64
	itxn_field XferAsset

	// Fee field not set, defaulting to 0
	intc 0 // 0
	itxn_field Fee

	// Submit inner transaction
	itxn_submit

	// contracts/AssetVault.algo.ts:290
	// return amountOut;
	frame_dig 8 // amountOut: uint64

	// set the subroutine return value
	frame_bury 0

	// pop all local variables from the stack
	popn 8
	retsub

// changeWeights(uint64,uint64[])uint64
*abi_route_changeWeights:
	// The ABI return prefix
	bytec 6 // 0x151f7c75

	// newWeights: uint64[]
	txna ApplicationArgs 2
	extract 2 0

	// duration: uint64
	txna ApplicationArgs 1
	btoi

	// execute changeWeights(uint64,uint64[])uint64
	callsub changeWeights
	itob
	concat
	log
	intc 2 // 1
	return

// changeWeights(duration: uint64, newWeights: uint64[]): uint64
//
// Change the pool's weights. Manager-only; rejected while a transition is active.
//
// With `duration === 0` the new weights apply immediately. Otherwise they
// interpolate linearly from the current weights to `newWeights` over `duration`
// blocks; during the transition {@link getCurrentWeight} returns the live value.
//
// @param duration   - interpolation length in blocks (0 = instant).
// @param newWeights - target weight per asset (each >= MIN_WEIGHT, summing to SCALE).
// @returns the round at which the transition ends (0 if instant).
changeWeights:
	proto 2 1

	// Push empty bytes after the frame pointer to reserve space for local variables
	bytec 0 // 0x
	dupn 4

	// contracts/AssetVault.algo.ts:305
	// this.assertIsManager()
	callsub assertIsManager

	// contracts/AssetVault.algo.ts:306
	// this.assertIsBootstrapped()
	callsub assertIsBootstrapped

	// contracts/AssetVault.algo.ts:307
	// this.assertNoWeightTransition()
	callsub assertNoWeightTransition

	// contracts/AssetVault.algo.ts:309
	// assert(newWeights.length === this.numAssets.value, 'weights length must match assets')
	frame_dig -2 // newWeights: uint64[]
	len
	intc 1 // 8
	/
	bytec 2 //  "num_assets"
	app_global_get
	==

	// weights length must match assets
	assert

	// contracts/AssetVault.algo.ts:311
	// sumOfWeights: uint64 = 0
	intc 0 // 0
	frame_bury 0 // sumOfWeights: uint64

	// contracts/AssetVault.algo.ts:312
	// for (let i = 0; i < newWeights.length; i += 1)
	intc 0 // 0
	frame_bury 1 // i: uint64

*for_5:
	// contracts/AssetVault.algo.ts:312
	// i < newWeights.length
	frame_dig 1 // i: uint64
	frame_dig -2 // newWeights: uint64[]
	len
	intc 1 // 8
	/
	<
	bz *for_5_end

	// contracts/AssetVault.algo.ts:313
	// assert(newWeights[i] >= MIN_WEIGHT, 'weight too small')
	frame_dig -2 // newWeights: uint64[]
	store 255 // full array
	intc 0 //  initial offset
	frame_dig 1 // i: uint64
	intc 1 // 8
	* // acc * typeLength
	+
	load 255 // full array
	swap
	intc 1 // 8
	extract3
	btoi
	intc 8 // 10000
	>=

	// weight too small
	assert

	// contracts/AssetVault.algo.ts:314
	// sumOfWeights += newWeights[i]
	frame_dig 0 // sumOfWeights: uint64
	frame_dig -2 // newWeights: uint64[]
	store 255 // full array
	intc 0 //  initial offset
	frame_dig 1 // i: uint64
	intc 1 // 8
	* // acc * typeLength
	+
	load 255 // full array
	swap
	intc 1 // 8
	extract3
	btoi
	+
	frame_bury 0 // sumOfWeights: uint64

*for_5_continue:
	// contracts/AssetVault.algo.ts:312
	// i += 1
	frame_dig 1 // i: uint64
	intc 2 // 1
	+
	frame_bury 1 // i: uint64
	b *for_5

*for_5_end:
	// contracts/AssetVault.algo.ts:316
	// assert(this.absDiff(sumOfWeights, SCALE) <= 1, 'weights must sum to 1')
	intc 3 // 1000000
	frame_dig 0 // sumOfWeights: uint64
	callsub absDiff
	intc 2 // 1
	<=

	// weights must sum to 1
	assert

	// *if2_condition
	// contracts/AssetVault.algo.ts:318
	// duration === 0
	frame_dig -1 // duration: uint64
	intc 0 // 0
	==
	bz *if2_else

	// *if2_consequent
	// contracts/AssetVault.algo.ts:319
	// this.startRound.value = 0
	bytec 8 //  "start_round"
	intc 0 // 0
	app_global_put

	// contracts/AssetVault.algo.ts:320
	// this.endRound.value = 0
	bytec 7 //  "end_round"
	intc 0 // 0
	app_global_put

	// contracts/AssetVault.algo.ts:321
	// for (let i = 0; i < newWeights.length; i += 1)
	intc 0 // 0
	frame_bury 2 // i: uint64

*for_6:
	// contracts/AssetVault.algo.ts:321
	// i < newWeights.length
	frame_dig 2 // i: uint64
	frame_dig -2 // newWeights: uint64[]
	len
	intc 1 // 8
	/
	<
	bz *for_6_end

	// contracts/AssetVault.algo.ts:322
	// this.weights(i).value = newWeights[i]
	bytec 9 //  "weights_"
	frame_dig 2 // i: uint64
	itob
	concat
	frame_dig -2 // newWeights: uint64[]
	store 255 // full array
	intc 0 //  initial offset
	frame_dig 2 // i: uint64
	intc 1 // 8
	* // acc * typeLength
	+
	load 255 // full array
	swap
	intc 1 // 8
	extract3
	btoi
	itob
	box_put

*for_6_continue:
	// contracts/AssetVault.algo.ts:321
	// i += 1
	frame_dig 2 // i: uint64
	intc 2 // 1
	+
	frame_bury 2 // i: uint64
	b *for_6

*for_6_end:
	b *if2_end

*if2_else:
	// contracts/AssetVault.algo.ts:325
	// currentRound = globals.round
	global Round
	frame_bury 3 // currentRound: uint64

	// contracts/AssetVault.algo.ts:327
	// this.startRound.value = currentRound
	bytec 8 //  "start_round"
	frame_dig 3 // currentRound: uint64
	app_global_put

	// contracts/AssetVault.algo.ts:328
	// this.endRound.value = currentRound + duration
	bytec 7 //  "end_round"
	frame_dig 3 // currentRound: uint64
	frame_dig -1 // duration: uint64
	+
	app_global_put

	// contracts/AssetVault.algo.ts:330
	// for (let i = 0; i < newWeights.length; i += 1)
	intc 0 // 0
	frame_bury 4 // i: uint64

*for_7:
	// contracts/AssetVault.algo.ts:330
	// i < newWeights.length
	frame_dig 4 // i: uint64
	frame_dig -2 // newWeights: uint64[]
	len
	intc 1 // 8
	/
	<
	bz *for_7_end

	// contracts/AssetVault.algo.ts:331
	// this.targetWeights(i).value = newWeights[i]
	bytec 11 //  "target_weights_"
	frame_dig 4 // i: uint64
	itob
	concat
	frame_dig -2 // newWeights: uint64[]
	store 255 // full array
	intc 0 //  initial offset
	frame_dig 4 // i: uint64
	intc 1 // 8
	* // acc * typeLength
	+
	load 255 // full array
	swap
	intc 1 // 8
	extract3
	btoi
	itob
	box_put

*for_7_continue:
	// contracts/AssetVault.algo.ts:330
	// i += 1
	frame_dig 4 // i: uint64
	intc 2 // 1
	+
	frame_bury 4 // i: uint64
	b *for_7

*for_7_end:

*if2_end:
	// contracts/AssetVault.algo.ts:335
	// return this.endRound.value;
	bytec 7 //  "end_round"
	app_global_get

	// set the subroutine return value
	frame_bury 0

	// pop all local variables from the stack
	popn 4
	retsub

// tryFinalizeWeights(): void
//
// Commit a finished weight interpolation into the stored weights.
tryFinalizeWeights:
	proto 0 0

	// Push empty bytes after the frame pointer to reserve space for local variables
	bytec 0 // 0x

	// *if3_condition
	// contracts/AssetVault.algo.ts:340
	// this.endRound.value !== 0 && globals.round >= this.endRound.value
	bytec 7 //  "end_round"
	app_global_get
	intc 0 // 0
	!=
	dup
	bz *skip_and0
	global Round
	bytec 7 //  "end_round"
	app_global_get
	>=
	&&

*skip_and0:
	bz *if3_end

	// *if3_consequent
	// contracts/AssetVault.algo.ts:341
	// for (let i = 0; i < this.numAssets.value; i += 1)
	intc 0 // 0
	frame_bury 0 // i: uint64

*for_8:
	// contracts/AssetVault.algo.ts:341
	// i < this.numAssets.value
	frame_dig 0 // i: uint64
	bytec 2 //  "num_assets"
	app_global_get
	<
	bz *for_8_end

	// contracts/AssetVault.algo.ts:342
	// this.weights(i).value = this.targetWeights(i).value
	bytec 9 //  "weights_"
	frame_dig 0 // i: uint64
	itob
	concat
	bytec 11 //  "target_weights_"
	frame_dig 0 // i: uint64
	itob
	concat
	box_get

	// box value does not exist: this.targetWeights(i).value
	assert
	btoi
	itob
	box_put

*for_8_continue:
	// contracts/AssetVault.algo.ts:341
	// i += 1
	frame_dig 0 // i: uint64
	intc 2 // 1
	+
	frame_bury 0 // i: uint64
	b *for_8

*for_8_end:
	// contracts/AssetVault.algo.ts:344
	// this.startRound.value = 0
	bytec 8 //  "start_round"
	intc 0 // 0
	app_global_put

	// contracts/AssetVault.algo.ts:345
	// this.endRound.value = 0
	bytec 7 //  "end_round"
	intc 0 // 0
	app_global_put

*if3_end:
	retsub

// optIn(assetId: AssetID): void
//
// *******************
// SUBROUTINES
// *******************
// Opt the app into an ASA if it is not already opted in.
optIn:
	proto 1 0

	// *if4_condition
	// contracts/AssetVault.algo.ts:355
	// this.app.address.isOptedInToAsset(assetId)
	global CurrentApplicationAddress
	frame_dig -1 // assetId: AssetID
	asset_holding_get AssetBalance
	swap
	pop
	bz *if4_end

	// *if4_consequent
	// contracts/AssetVault.algo.ts:356
	// return;
	retsub

*if4_end:
	// contracts/AssetVault.algo.ts:359
	// sendAssetTransfer({
	//       assetReceiver: this.app.address,
	//       xferAsset: assetId,
	//       assetAmount: 0,
	//     })
	itxn_begin
	intc 4 //  axfer
	itxn_field TypeEnum

	// contracts/AssetVault.algo.ts:360
	// assetReceiver: this.app.address
	global CurrentApplicationAddress
	itxn_field AssetReceiver

	// contracts/AssetVault.algo.ts:361
	// xferAsset: assetId
	frame_dig -1 // assetId: AssetID
	itxn_field XferAsset

	// contracts/AssetVault.algo.ts:362
	// assetAmount: 0
	intc 0 // 0
	itxn_field AssetAmount

	// Fee field not set, defaulting to 0
	intc 0 // 0
	itxn_field Fee

	// Submit inner transaction
	itxn_submit
	retsub

// addToken(index: uint64, assetID: AssetID, weight: uint64): void
//
// Create the asset/weight/balance boxes for one token at `index`.
addToken:
	proto 3 0

	// *if5_condition
	// contracts/AssetVault.algo.ts:368
	// !this.assetAt(index).exists
	bytec 3 //  "asset_"
	frame_dig -1 // index: uint64
	itob
	concat
	box_len
	swap
	pop
	!
	bz *if5_end

	// *if5_consequent
	// contracts/AssetVault.algo.ts:369
	// this.assetAt(index).create(8)
	bytec 3 //  "asset_"
	frame_dig -1 // index: uint64
	itob
	concat
	intc 1 // 8
	box_create
	pop

*if5_end:
	// *if6_condition
	// contracts/AssetVault.algo.ts:372
	// !this.weights(index).exists
	bytec 9 //  "weights_"
	frame_dig -1 // index: uint64
	itob
	concat
	box_len
	swap
	pop
	!
	bz *if6_end

	// *if6_consequent
	// contracts/AssetVault.algo.ts:373
	// this.weights(index).create(8)
	bytec 9 //  "weights_"
	frame_dig -1 // index: uint64
	itob
	concat
	intc 1 // 8
	box_create
	pop

*if6_end:
	// *if7_condition
	// contracts/AssetVault.algo.ts:376
	// !this.balances(assetID).exists
	bytec 1 //  "balances_"
	frame_dig -2 // assetID: AssetID
	itob
	concat
	box_len
	swap
	pop
	!
	bz *if7_end

	// *if7_consequent
	// contracts/AssetVault.algo.ts:377
	// this.balances(assetID).create(8)
	bytec 1 //  "balances_"
	frame_dig -2 // assetID: AssetID
	itob
	concat
	intc 1 // 8
	box_create
	pop

*if7_end:
	// contracts/AssetVault.algo.ts:380
	// this.assetAt(index).value = assetID
	bytec 3 //  "asset_"
	frame_dig -1 // index: uint64
	itob
	concat
	frame_dig -2 // assetID: AssetID
	itob
	box_put

	// contracts/AssetVault.algo.ts:381
	// this.weights(index).value = weight
	bytec 9 //  "weights_"
	frame_dig -1 // index: uint64
	itob
	concat
	frame_dig -3 // weight: uint64
	itob
	box_put

	// contracts/AssetVault.algo.ts:382
	// this.balances(assetID).value = 0
	bytec 1 //  "balances_"
	frame_dig -2 // assetID: AssetID
	itob
	concat
	bytec 14 // 0x0000000000000000
	box_put
	retsub

// createToken(): void
//
// Create the pool's LP token (once). It is an ASA with this contract as manager
// and reserve, 6 decimals, and no clawback/freeze. Held in reserve and released
// as liquidity is minted.
createToken:
	proto 0 0

	// *if8_condition
	// contracts/AssetVault.algo.ts:391
	// this.token.value === AssetID.zeroIndex
	bytec 4 //  "token"
	app_global_get
	intc 0 // 0
	==
	bz *if8_end

	// *if8_consequent
	// contracts/AssetVault.algo.ts:392
	// this.token.value = sendAssetCreation({
	//         configAssetTotal: TOTAL_LP_SUPPLY,
	//         configAssetDecimals: 6,
	//         configAssetReserve: this.app.address,
	//         configAssetManager: this.app.address,
	//         configAssetClawback: globals.zeroAddress,
	//         configAssetFreeze: globals.zeroAddress,
	//         configAssetDefaultFrozen: 0,
	//         configAssetName: 'BalancedPool-' + this.app.id.toString(),
	//         configAssetUnitName: 'LP',
	//       })
	bytec 4 //  "token"
	itxn_begin
	pushint 3 // acfg
	itxn_field TypeEnum

	// contracts/AssetVault.algo.ts:393
	// configAssetTotal: TOTAL_LP_SUPPLY
	intc 5 // 10
	pushint 16
	exp
	itxn_field ConfigAssetTotal

	// contracts/AssetVault.algo.ts:394
	// configAssetDecimals: 6
	intc 6 // 6
	itxn_field ConfigAssetDecimals

	// contracts/AssetVault.algo.ts:395
	// configAssetReserve: this.app.address
	global CurrentApplicationAddress
	itxn_field ConfigAssetReserve

	// contracts/AssetVault.algo.ts:396
	// configAssetManager: this.app.address
	global CurrentApplicationAddress
	itxn_field ConfigAssetManager

	// contracts/AssetVault.algo.ts:397
	// configAssetClawback: globals.zeroAddress
	global ZeroAddress
	itxn_field ConfigAssetClawback

	// contracts/AssetVault.algo.ts:398
	// configAssetFreeze: globals.zeroAddress
	global ZeroAddress
	itxn_field ConfigAssetFreeze

	// contracts/AssetVault.algo.ts:399
	// configAssetDefaultFrozen: 0
	intc 0 // 0
	itxn_field ConfigAssetDefaultFrozen

	// contracts/AssetVault.algo.ts:400
	// configAssetName: 'BalancedPool-' + this.app.id.toString()
	pushbytes 0x42616c616e636564506f6f6c2d // "BalancedPool-"
	txna Applications 0
	callsub *itoa
	concat
	itxn_field ConfigAssetName

	// contracts/AssetVault.algo.ts:401
	// configAssetUnitName: 'LP'
	pushbytes 0x4c50 // "LP"
	itxn_field ConfigAssetUnitName

	// Fee field not set, defaulting to 0
	intc 0 // 0
	itxn_field Fee

	// Submit inner transaction
	itxn_submit
	itxn CreatedAssetID
	app_global_put

*if8_end:
	retsub

// assertIsManager(): void
assertIsManager:
	proto 0 0

	// contracts/AssetVault.algo.ts:407
	// assert(this.txn.sender === this.manager.value, 'only the manager can call this method')
	txn Sender
	bytec 15 //  "manager"
	app_global_get
	==

	// only the manager can call this method
	assert
	retsub

// assertIsBootstrapped(): void
assertIsBootstrapped:
	proto 0 0

	// contracts/AssetVault.algo.ts:411
	// assert(this.token.value !== AssetID.zeroIndex, 'pool not bootstrapped')
	bytec 4 //  "token"
	app_global_get
	intc 0 // 0
	!=

	// pool not bootstrapped
	assert
	retsub

// assertNoWeightTransition(): void
assertNoWeightTransition:
	proto 0 0

	// contracts/AssetVault.algo.ts:415
	// assert(this.startRound.value === 0 && this.startRound.value === this.endRound.value)
	bytec 8 //  "start_round"
	app_global_get
	intc 0 // 0
	==
	dup
	bz *skip_and1
	bytec 8 //  "start_round"
	app_global_get
	bytec 7 //  "end_round"
	app_global_get
	==
	&&

*skip_and1:
	assert
	retsub

// lnWithSign(x: uint64): uint64[]
//
// Fixed-point ln(x) via the Mercator series around 1, with a sign flag.
// For x < 1 it computes ln(1/x) and flags the result negative, keeping precision.
//
// @param x - value scaled by SCALE (must be > 0).
// @returns [negative, |ln(x)| scaled by SCALE], negative = 1 when ln(x) < 0.
lnWithSign:
	proto 1 1

	// Push empty bytes after the frame pointer to reserve space for local variables
	bytec 0 // 0x
	dupn 5

	// contracts/AssetVault.algo.ts:426
	// assert(x > 0, 'log undefined for x ≤ 0')
	frame_dig -1 // x: uint64
	intc 0 // 0
	>

	// log undefined for x ≤ 0
	assert

	// contracts/AssetVault.algo.ts:428
	// negative: uint64 = 0
	intc 0 // 0
	frame_bury 0 // negative: uint64

	// *if9_condition
	// contracts/AssetVault.algo.ts:431
	// x < SCALE
	frame_dig -1 // x: uint64
	intc 3 // 1000000
	<
	bz *if9_else

	// *if9_consequent
	// contracts/AssetVault.algo.ts:432
	// negative = 1
	intc 2 // 1
	frame_bury 0 // negative: uint64

	// contracts/AssetVault.algo.ts:433
	// invX = wideRatio([SCALE, SCALE], [x])
	intc 3 // 1000000
	dup
	mulw
	intc 0 // 0
	frame_dig -1 // x: uint64
	divmodw
	pop
	pop
	swap
	!

	// wideRatio failed
	assert
	frame_bury 2 // invX: uint64

	// contracts/AssetVault.algo.ts:434
	// z = wideRatio([invX - SCALE, SCALE], [invX])
	frame_dig 2 // invX: uint64
	intc 3 // 1000000
	-
	intc 3 // 1000000
	mulw
	intc 0 // 0
	frame_dig 2 // invX: uint64
	divmodw
	pop
	pop
	swap
	!

	// wideRatio failed
	assert
	frame_bury 1 // z: uint64
	b *if9_end

*if9_else:
	// contracts/AssetVault.algo.ts:436
	// z = wideRatio([x - SCALE, SCALE], [x])
	frame_dig -1 // x: uint64
	intc 3 // 1000000
	-
	intc 3 // 1000000
	mulw
	intc 0 // 0
	frame_dig -1 // x: uint64
	divmodw
	pop
	pop
	swap
	!

	// wideRatio failed
	assert
	frame_bury 1 // z: uint64

*if9_end:
	// contracts/AssetVault.algo.ts:440
	// result = z
	frame_dig 1 // z: uint64
	frame_bury 3 // result: uint64

	// contracts/AssetVault.algo.ts:441
	// term = z
	frame_dig 1 // z: uint64
	frame_bury 4 // term: uint64

	// contracts/AssetVault.algo.ts:443
	// increaseOpcodeBudget()
	itxn_begin
	intc 6 //  appl
	itxn_field TypeEnum
	intc 0 // 0
	itxn_field Fee
	bytec 10 //  #pragma version 10; int 1
	dup
	itxn_field ApprovalProgram
	itxn_field ClearStateProgram
	intc 7 //  DeleteApplication
	itxn_field OnCompletion
	itxn_submit

	// contracts/AssetVault.algo.ts:445
	// for (let i = 2; i <= 10; i = i + 1)
	pushint 2
	frame_bury 5 // i: uint64

*for_9:
	// contracts/AssetVault.algo.ts:445
	// i <= 10
	frame_dig 5 // i: uint64
	intc 5 // 10
	<=
	bz *for_9_end

	// contracts/AssetVault.algo.ts:446
	// term = wideRatio([term, z], [SCALE])
	frame_dig 4 // term: uint64
	frame_dig 1 // z: uint64
	mulw
	intc 0 // 0
	intc 3 // 1000000
	divmodw
	pop
	pop
	swap
	!

	// wideRatio failed
	assert
	frame_bury 4 // term: uint64

	// contracts/AssetVault.algo.ts:447
	// result = result + wideRatio([term], [i])
	frame_dig 3 // result: uint64
	intc 0 // 0
	frame_dig 4 // term: uint64
	intc 0 // 0
	frame_dig 5 // i: uint64
	divmodw
	pop
	pop
	swap
	!

	// wideRatio failed
	assert
	+
	frame_bury 3 // result: uint64

*for_9_continue:
	// contracts/AssetVault.algo.ts:445
	// i = i + 1
	frame_dig 5 // i: uint64
	intc 2 // 1
	+
	frame_bury 5 // i: uint64
	b *for_9

*for_9_end:
	// contracts/AssetVault.algo.ts:450
	// return [negative, result];
	frame_dig 0 // negative: uint64
	itob
	frame_dig 3 // result: uint64
	itob
	concat

	// set the subroutine return value
	frame_bury 0

	// pop all local variables from the stack
	popn 5
	retsub

// exp(x: uint64): uint64
//
// Fixed-point e^x via a 10-term Taylor series.
//
// @param x - exponent scaled by SCALE.
// @returns e^x scaled by SCALE.
exp:
	proto 1 1

	// Push empty bytes after the frame pointer to reserve space for local variables
	bytec 0 // 0x
	dupn 2

	// contracts/AssetVault.algo.ts:460
	// result = SCALE
	intc 3 // 1000000
	frame_bury 0 // result: uint64

	// contracts/AssetVault.algo.ts:461
	// term = SCALE
	intc 3 // 1000000
	frame_bury 1 // term: uint64

	// contracts/AssetVault.algo.ts:463
	// for (let i = 1; i <= 10; i = i + 1)
	intc 2 // 1
	frame_bury 2 // i: uint64

*for_10:
	// contracts/AssetVault.algo.ts:463
	// i <= 10
	frame_dig 2 // i: uint64
	intc 5 // 10
	<=
	bz *for_10_end

	// contracts/AssetVault.algo.ts:464
	// term = wideRatio([term, x], [i * SCALE])
	frame_dig 1 // term: uint64
	frame_dig -1 // x: uint64
	mulw
	intc 0 // 0
	frame_dig 2 // i: uint64
	intc 3 // 1000000
	*
	divmodw
	pop
	pop
	swap
	!

	// wideRatio failed
	assert
	frame_bury 1 // term: uint64

	// contracts/AssetVault.algo.ts:465
	// result += term
	frame_dig 0 // result: uint64
	frame_dig 1 // term: uint64
	+
	frame_bury 0 // result: uint64

*for_10_continue:
	// contracts/AssetVault.algo.ts:463
	// i = i + 1
	frame_dig 2 // i: uint64
	intc 2 // 1
	+
	frame_bury 2 // i: uint64
	b *for_10

*for_10_end:
	// contracts/AssetVault.algo.ts:468
	// return result;
	frame_dig 0 // result: uint64

	// set the subroutine return value
	frame_bury 0

	// pop all local variables from the stack
	popn 2
	retsub

// pow(x: uint64, y: uint64): uint64
//
// Fixed-point x^y, computed as exp(y * ln(x)) with sign-aware ln for x < 1.
// Accuracy degrades for ratios far from 1 (series truncation).
//
// @param x - base scaled by SCALE.
// @param y - exponent scaled by SCALE.
// @returns x^y scaled by SCALE.
pow:
	proto 2 1

	// Push empty bytes after the frame pointer to reserve space for local variables
	bytec 0 // 0x
	dupn 4

	// *if10_condition
	// contracts/AssetVault.algo.ts:480
	// x === 0
	frame_dig -1 // x: uint64
	intc 0 // 0
	==
	bz *if10_end

	// *if10_consequent
	// contracts/AssetVault.algo.ts:480
	// return 0;
	intc 0 // 0
	b *pow*return

*if10_end:
	// contracts/AssetVault.algo.ts:482
	// lnXResult = this.lnWithSign(x)
	frame_dig -1 // x: uint64
	callsub lnWithSign
	frame_bury 0 // lnXResult: uint64[]

	// contracts/AssetVault.algo.ts:483
	// negativeLn = lnXResult[0]
	frame_dig 0 // lnXResult: uint64[]
	store 255 // full array
	load 255 // full array
	extract 0 8
	btoi
	frame_bury 1 // negativeLn: uint64

	// contracts/AssetVault.algo.ts:484
	// lnX = lnXResult[1]
	frame_dig 0 // lnXResult: uint64[]
	store 255 // full array
	load 255 // full array
	extract 8 8
	btoi
	frame_bury 2 // lnX: uint64

	// contracts/AssetVault.algo.ts:486
	// ylnX = wideRatio([y, lnX], [SCALE])
	frame_dig -2 // y: uint64
	frame_dig 2 // lnX: uint64
	mulw
	intc 0 // 0
	intc 3 // 1000000
	divmodw
	pop
	pop
	swap
	!

	// wideRatio failed
	assert
	frame_bury 3 // ylnX: uint64

	// contracts/AssetVault.algo.ts:488
	// expResult = this.exp(ylnX)
	frame_dig 3 // ylnX: uint64
	callsub exp
	frame_bury 4 // expResult: uint64

	// *if11_condition
	// contracts/AssetVault.algo.ts:490
	// negativeLn === 1
	frame_dig 1 // negativeLn: uint64
	intc 2 // 1
	==
	bz *if11_end

	// *if11_consequent
	// contracts/AssetVault.algo.ts:491
	// return wideRatio([SCALE, SCALE], [expResult]);
	intc 3 // 1000000
	dup
	mulw
	intc 0 // 0
	frame_dig 4 // expResult: uint64
	divmodw
	pop
	pop
	swap
	!

	// wideRatio failed
	assert
	b *pow*return

*if11_end:
	// contracts/AssetVault.algo.ts:494
	// return expResult;
	frame_dig 4 // expResult: uint64

*pow*return:
	// set the subroutine return value
	frame_bury 0

	// pop all local variables from the stack
	popn 4
	retsub

// calcOut(balanceIn: uint64, weightIn: uint64, balanceOut: uint64, weightOut: uint64, amountIn: uint64): uint64
//
// Weighted constant-mean swap output, with a swap fee on the input:
//
// amountOut = balanceOut * (1 - (balanceIn / (balanceIn + amountInWithFee)) ^ (weightIn / weightOut))
//
// @param balanceIn  - input asset balance.
// @param weightIn   - input asset weight (scaled by SCALE).
// @param balanceOut - output asset balance.
// @param weightOut  - output asset weight (scaled by SCALE).
// @param amountIn   - input amount sent by the user.
// @returns the output amount.
calcOut:
	proto 5 1

	// Push empty bytes after the frame pointer to reserve space for local variables
	bytec 0 // 0x
	dupn 3

	// contracts/AssetVault.algo.ts:518
	// amountInWithFee = wideRatio([amountIn, SCALE - fee], [SCALE])
	frame_dig -5 // amountIn: uint64
	pushint 999000
	mulw
	intc 0 // 0
	intc 3 // 1000000
	divmodw
	pop
	pop
	swap
	!

	// wideRatio failed
	assert
	frame_bury 0 // amountInWithFee: uint64

	// contracts/AssetVault.algo.ts:520
	// ratio = wideRatio([balanceIn, SCALE], [balanceIn + amountInWithFee])
	frame_dig -1 // balanceIn: uint64
	intc 3 // 1000000
	mulw
	intc 0 // 0
	frame_dig -1 // balanceIn: uint64
	frame_dig 0 // amountInWithFee: uint64
	+
	divmodw
	pop
	pop
	swap
	!

	// wideRatio failed
	assert
	frame_bury 1 // ratio: uint64

	// contracts/AssetVault.algo.ts:522
	// power = wideRatio([weightIn, SCALE], [weightOut])
	frame_dig -2 // weightIn: uint64
	intc 3 // 1000000
	mulw
	intc 0 // 0
	frame_dig -4 // weightOut: uint64
	divmodw
	pop
	pop
	swap
	!

	// wideRatio failed
	assert
	frame_bury 2 // power: uint64

	// contracts/AssetVault.algo.ts:524
	// ratioPow = this.pow(ratio, power)
	frame_dig 2 // power: uint64
	frame_dig 1 // ratio: uint64
	callsub pow
	frame_bury 3 // ratioPow: uint64

	// contracts/AssetVault.algo.ts:526
	// return wideRatio([balanceOut, SCALE - ratioPow], [SCALE]);
	frame_dig -3 // balanceOut: uint64
	intc 3 // 1000000
	frame_dig 3 // ratioPow: uint64
	-
	mulw
	intc 0 // 0
	intc 3 // 1000000
	divmodw
	pop
	pop
	swap
	!

	// wideRatio failed
	assert

	// set the subroutine return value
	frame_bury 0

	// pop all local variables from the stack
	popn 3
	retsub

// computeNAssetsLiquidity(sender: Address): uint64
//
// LP to mint for a proportional deposit.
//
// Every asset must grow by the same fraction k of its balance (within 0.5%),
// otherwise the deposit is not invariant-preserving and is rejected — this also
// blocks single-sided deposits. Because the deposit grows the invariant by
// (1 + k) and Σ weights = 1, the LP supply must grow by exactly k:
//
// minted = totalLP * k
//
// This is linear and exact — no per-asset pow is needed once proportionality is
// enforced. Clears the sender's `provided` slots as it reads them.
//
// @param sender - the provider whose deposit is being priced.
// @returns the LP amount to mint.
computeNAssetsLiquidity:
	proto 1 1

	// Push empty bytes after the frame pointer to reserve space for local variables
	bytec 0 // 0x
	dupn 6

	// contracts/AssetVault.algo.ts:546
	// totalAssets = this.numAssets.value
	bytec 2 //  "num_assets"
	app_global_get
	frame_bury 0 // totalAssets: uint64

	// contracts/AssetVault.algo.ts:547
	// assert(totalAssets >= 1, 'Please provide at least one asset')
	frame_dig 0 // totalAssets: uint64
	intc 2 // 1
	>=

	// Please provide at least one asset
	assert

	// contracts/AssetVault.algo.ts:549
	// referenceRatio: uint64 = 0
	intc 0 // 0
	frame_bury 1 // referenceRatio: uint64

	// contracts/AssetVault.algo.ts:551
	// for (let i = 0; i < totalAssets; i += 1)
	intc 0 // 0
	frame_bury 2 // i: uint64

*for_11:
	// contracts/AssetVault.algo.ts:551
	// i < totalAssets
	frame_dig 2 // i: uint64
	frame_dig 0 // totalAssets: uint64
	<
	bz *for_11_end

	// contracts/AssetVault.algo.ts:552
	// assetId = this.assetAt(i).value
	bytec 3 //  "asset_"
	frame_dig 2 // i: uint64
	itob
	concat
	box_get

	// box value does not exist: this.assetAt(i).value
	assert
	btoi
	frame_bury 3 // assetId: uint64

	// contracts/AssetVault.algo.ts:553
	// poolBalance = this.balances(assetId).value
	bytec 1 //  "balances_"
	frame_dig 3 // assetId: uint64
	itob
	concat
	box_get

	// box value does not exist: this.balances(assetId).value
	assert
	btoi
	frame_bury 4 // poolBalance: uint64

	// contracts/AssetVault.algo.ts:554
	// providedAmount = this.provided(sender).value[i]
	bytec 5 //  "provided_"
	frame_dig -1 // sender: Address
	concat
	box_get

	// box value does not exist: this.provided(sender).value
	assert
	extract 2 0
	store 255 // full array
	intc 0 //  initial offset
	frame_dig 2 // i: uint64
	intc 1 // 8
	* // acc * typeLength
	+
	load 255 // full array
	swap
	intc 1 // 8
	extract3
	btoi
	frame_bury 5 // providedAmount: uint64

	// contracts/AssetVault.algo.ts:556
	// assert(poolBalance > 0, 'Pool balance must be > 0')
	frame_dig 4 // poolBalance: uint64
	intc 0 // 0
	>

	// Pool balance must be > 0
	assert

	// contracts/AssetVault.algo.ts:559
	// assetRatio = wideRatio([providedAmount, SCALE], [poolBalance - providedAmount])
	frame_dig 5 // providedAmount: uint64
	intc 3 // 1000000
	mulw
	intc 0 // 0
	frame_dig 4 // poolBalance: uint64
	frame_dig 5 // providedAmount: uint64
	-
	divmodw
	pop
	pop
	swap
	!

	// wideRatio failed
	assert
	frame_bury 6 // assetRatio: uint64

	// *if12_condition
	// contracts/AssetVault.algo.ts:562
	// i === 0
	frame_dig 2 // i: uint64
	intc 0 // 0
	==
	bz *if12_else

	// *if12_consequent
	// contracts/AssetVault.algo.ts:563
	// referenceRatio = assetRatio
	frame_dig 6 // assetRatio: uint64
	frame_bury 1 // referenceRatio: uint64
	b *if12_end

*if12_else:
	// contracts/AssetVault.algo.ts:565
	// assert(
	//           this.absDiff(assetRatio, referenceRatio) <= referenceRatio / 200,
	//           'deposit must be proportional to pool balances'
	//         )
	frame_dig 1 // referenceRatio: uint64
	frame_dig 6 // assetRatio: uint64
	callsub absDiff
	frame_dig 1 // referenceRatio: uint64
	pushint 200
	/
	<=

	// deposit must be proportional to pool balances
	assert

*if12_end:
	// contracts/AssetVault.algo.ts:571
	// this.provided(sender).value[i] = 0
	bytec 5 //  "provided_"
	frame_dig -1 // sender: Address
	concat
	box_get

	// box value does not exist: this.provided(sender).value
	assert
	extract 2 0
	store 255 // full array
	intc 0 //  initial offset
	frame_dig 2 // i: uint64
	intc 1 // 8
	* // acc * typeLength
	+
	load 255 // full array
	swap
	bytec 14 // 0x0000000000000000
	replace3
	bytec 5 //  "provided_"
	frame_dig -1 // sender: Address
	concat
	dup
	box_del
	pop
	swap
	dup
	len
	intc 1 // 8
	/
	itob
	extract 6 2
	swap
	concat
	box_put

*for_11_continue:
	// contracts/AssetVault.algo.ts:551
	// i += 1
	frame_dig 2 // i: uint64
	intc 2 // 1
	+
	frame_bury 2 // i: uint64
	b *for_11

*for_11_end:
	// contracts/AssetVault.algo.ts:574
	// return wideRatio([this.totalLP(), referenceRatio], [SCALE]);
	callsub totalLP
	frame_dig 1 // referenceRatio: uint64
	mulw
	intc 0 // 0
	intc 3 // 1000000
	divmodw
	pop
	pop
	swap
	!

	// wideRatio failed
	assert

	// set the subroutine return value
	frame_bury 0

	// pop all local variables from the stack
	popn 6
	retsub

// totalLP(): uint64
//
// Circulating LP supply: total issued, minus the reserve, minus burned.
totalLP:
	proto 0 1

	// contracts/AssetVault.algo.ts:579
	// return this.token.value.total - this.token.value.reserve.assetBalance(this.token.value) - this.burned.value;
	bytec 4 //  "token"
	app_global_get
	asset_params_get AssetTotal
	pop
	bytec 4 //  "token"
	app_global_get
	asset_params_get AssetReserve
	pop
	bytec 4 //  "token"
	app_global_get
	asset_holding_get AssetBalance
	pop
	-
	bytec 16 //  "burned"
	app_global_get
	-
	retsub

// absDiff(a: uint64, b: uint64): uint64
//
// |a - b| for unsigned integers.
absDiff:
	proto 2 1

	// contracts/AssetVault.algo.ts:584
	// return a > b ? a - b : b - a;
	frame_dig -1 // a: uint64
	frame_dig -2 // b: uint64
	>
	bz *ternary0_false
	frame_dig -1 // a: uint64
	frame_dig -2 // b: uint64
	-
	b *ternary0_end

*ternary0_false:
	frame_dig -2 // b: uint64
	frame_dig -1 // a: uint64
	-

*ternary0_end:
	retsub

// getTotalAssets()uint64
*abi_route_getTotalAssets:
	// The ABI return prefix
	bytec 6 // 0x151f7c75

	// execute getTotalAssets()uint64
	callsub getTotalAssets
	itob
	concat
	log
	intc 2 // 1
	return

// getTotalAssets(): uint64
getTotalAssets:
	proto 0 1

	// contracts/AssetVault.algo.ts:589
	// return this.numAssets.value;
	bytec 2 //  "num_assets"
	app_global_get
	retsub

// getToken()uint64
*abi_route_getToken:
	// The ABI return prefix
	bytec 6 // 0x151f7c75

	// execute getToken()uint64
	callsub getToken
	itob
	concat
	log
	intc 2 // 1
	return

// getToken(): AssetID
getToken:
	proto 0 1

	// contracts/AssetVault.algo.ts:594
	// return this.token.value;
	bytec 4 //  "token"
	app_global_get
	retsub

// getWeight(uint64)uint64
*abi_route_getWeight:
	// The ABI return prefix
	bytec 6 // 0x151f7c75

	// index: uint64
	txna ApplicationArgs 1
	btoi

	// execute getWeight(uint64)uint64
	callsub getWeight
	itob
	concat
	log
	intc 2 // 1
	return

// getWeight(index: uint64): uint64
getWeight:
	proto 1 1

	// contracts/AssetVault.algo.ts:599
	// return this.weights(index).value;
	bytec 9 //  "weights_"
	frame_dig -1 // index: uint64
	itob
	concat
	box_get

	// box value does not exist: this.weights(index).value
	assert
	btoi
	retsub

// getBalance(uint64)uint64
*abi_route_getBalance:
	// The ABI return prefix
	bytec 6 // 0x151f7c75

	// index: uint64
	txna ApplicationArgs 1
	btoi

	// execute getBalance(uint64)uint64
	callsub getBalance
	itob
	concat
	log
	intc 2 // 1
	return

// getBalance(index: uint64): uint64
getBalance:
	proto 1 1

	// Push empty bytes after the frame pointer to reserve space for local variables
	bytec 0 // 0x

	// contracts/AssetVault.algo.ts:604
	// asset = this.assetAt(index).value
	bytec 3 //  "asset_"
	frame_dig -1 // index: uint64
	itob
	concat
	box_get

	// box value does not exist: this.assetAt(index).value
	assert
	btoi
	frame_bury 0 // asset: uint64

	// contracts/AssetVault.algo.ts:605
	// return this.balances(asset).value;
	bytec 1 //  "balances_"
	frame_dig 0 // asset: uint64
	itob
	concat
	box_get

	// box value does not exist: this.balances(asset).value
	assert
	btoi

	// set the subroutine return value
	frame_bury 0
	retsub

// estimateSwap(uint64,uint64,uint64)uint64
*abi_route_estimateSwap:
	// The ABI return prefix
	bytec 6 // 0x151f7c75

	// amount: uint64
	txna ApplicationArgs 3
	btoi

	// to: uint64
	txna ApplicationArgs 2
	btoi

	// from: uint64
	txna ApplicationArgs 1
	btoi

	// execute estimateSwap(uint64,uint64,uint64)uint64
	callsub estimateSwap
	itob
	concat
	log
	intc 2 // 1
	return

// estimateSwap(from: uint64, to: uint64, amount: uint64): uint64
estimateSwap:
	proto 3 1

	// Push empty bytes after the frame pointer to reserve space for local variables
	bytec 0 // 0x
	dupn 5

	// contracts/AssetVault.algo.ts:610
	// assetIn = this.assetAt(from).value
	bytec 3 //  "asset_"
	frame_dig -1 // from: uint64
	itob
	concat
	box_get

	// box value does not exist: this.assetAt(from).value
	assert
	btoi
	frame_bury 0 // assetIn: uint64

	// contracts/AssetVault.algo.ts:611
	// assetOut = this.assetAt(to).value
	bytec 3 //  "asset_"
	frame_dig -2 // to: uint64
	itob
	concat
	box_get

	// box value does not exist: this.assetAt(to).value
	assert
	btoi
	frame_bury 1 // assetOut: uint64

	// contracts/AssetVault.algo.ts:613
	// balanceIn = this.balances(assetIn).value
	bytec 1 //  "balances_"
	frame_dig 0 // assetIn: uint64
	itob
	concat
	box_get

	// box value does not exist: this.balances(assetIn).value
	assert
	btoi
	frame_bury 2 // balanceIn: uint64

	// contracts/AssetVault.algo.ts:614
	// balanceOut = this.balances(assetOut).value
	bytec 1 //  "balances_"
	frame_dig 1 // assetOut: uint64
	itob
	concat
	box_get

	// box value does not exist: this.balances(assetOut).value
	assert
	btoi
	frame_bury 3 // balanceOut: uint64

	// contracts/AssetVault.algo.ts:616
	// weightIn = this.getCurrentWeight(from)
	frame_dig -1 // from: uint64
	callsub getCurrentWeight
	frame_bury 4 // weightIn: uint64

	// contracts/AssetVault.algo.ts:617
	// weightOut = this.getCurrentWeight(to)
	frame_dig -2 // to: uint64
	callsub getCurrentWeight
	frame_bury 5 // weightOut: uint64

	// contracts/AssetVault.algo.ts:619
	// return this.calcOut(balanceIn, weightIn, balanceOut, weightOut, amount);
	frame_dig -3 // amount: uint64
	frame_dig 5 // weightOut: uint64
	frame_dig 3 // balanceOut: uint64
	frame_dig 4 // weightIn: uint64
	frame_dig 2 // balanceIn: uint64
	callsub calcOut

	// set the subroutine return value
	frame_bury 0

	// pop all local variables from the stack
	popn 5
	retsub

// getCurrentWeight(uint64)uint64
*abi_route_getCurrentWeight:
	// The ABI return prefix
	bytec 6 // 0x151f7c75

	// index: uint64
	txna ApplicationArgs 1
	btoi

	// execute getCurrentWeight(uint64)uint64
	callsub getCurrentWeight
	itob
	concat
	log
	intc 2 // 1
	return

// getCurrentWeight(index: uint64): uint64
//
// Current weight of an asset, interpolated if a weight transition is active.
getCurrentWeight:
	proto 1 1

	// Push empty bytes after the frame pointer to reserve space for local variables
	bytec 0 // 0x
	dupn 8

	// contracts/AssetVault.algo.ts:625
	// current = globals.round
	global Round
	frame_bury 0 // current: uint64

	// contracts/AssetVault.algo.ts:626
	// start = this.startRound.value
	bytec 8 //  "start_round"
	app_global_get
	frame_bury 1 // start: uint64

	// contracts/AssetVault.algo.ts:627
	// end = this.endRound.value
	bytec 7 //  "end_round"
	app_global_get
	frame_bury 2 // end: uint64

	// *if13_condition
	// contracts/AssetVault.algo.ts:629
	// current <= start || start === 0 || end === 0
	frame_dig 0 // current: uint64
	frame_dig 1 // start: uint64
	<=
	dup
	bnz *skip_or0
	frame_dig 1 // start: uint64
	intc 0 // 0
	==
	||

*skip_or0:
	dup
	bnz *skip_or1
	frame_dig 2 // end: uint64
	intc 0 // 0
	==
	||

*skip_or1:
	bz *if13_end

	// *if13_consequent
	// contracts/AssetVault.algo.ts:630
	// return this.weights(index).value;
	bytec 9 //  "weights_"
	frame_dig -1 // index: uint64
	itob
	concat
	box_get

	// box value does not exist: this.weights(index).value
	assert
	btoi
	b *getCurrentWeight*return

*if13_end:
	// *if14_condition
	// contracts/AssetVault.algo.ts:633
	// current >= end
	frame_dig 0 // current: uint64
	frame_dig 2 // end: uint64
	>=
	bz *if14_end

	// *if14_consequent
	// contracts/AssetVault.algo.ts:634
	// return this.targetWeights(index).value;
	bytec 11 //  "target_weights_"
	frame_dig -1 // index: uint64
	itob
	concat
	box_get

	// box value does not exist: this.targetWeights(index).value
	assert
	btoi
	b *getCurrentWeight*return

*if14_end:
	// contracts/AssetVault.algo.ts:637
	// elapsed = current - start
	frame_dig 0 // current: uint64
	frame_dig 1 // start: uint64
	-
	frame_bury 3 // elapsed: uint64

	// contracts/AssetVault.algo.ts:638
	// total = end - start
	frame_dig 2 // end: uint64
	frame_dig 1 // start: uint64
	-
	frame_bury 4 // total: uint64

	// contracts/AssetVault.algo.ts:640
	// w0 = this.weights(index).value
	bytec 9 //  "weights_"
	frame_dig -1 // index: uint64
	itob
	concat
	box_get

	// box value does not exist: this.weights(index).value
	assert
	btoi
	frame_bury 5 // w0: uint64

	// contracts/AssetVault.algo.ts:641
	// w1 = this.targetWeights(index).value
	bytec 11 //  "target_weights_"
	frame_dig -1 // index: uint64
	itob
	concat
	box_get

	// box value does not exist: this.targetWeights(index).value
	assert
	btoi
	frame_bury 6 // w1: uint64

	// contracts/AssetVault.algo.ts:643
	// delta = w1 > w0 ? w1 - w0 : w0 - w1
	frame_dig 6 // w1: uint64
	frame_dig 5 // w0: uint64
	>
	bz *ternary1_false
	frame_dig 6 // w1: uint64
	frame_dig 5 // w0: uint64
	-
	b *ternary1_end

*ternary1_false:
	frame_dig 5 // w0: uint64
	frame_dig 6 // w1: uint64
	-

*ternary1_end:
	frame_bury 7 // delta: uint64

	// contracts/AssetVault.algo.ts:644
	// offset = wideRatio([delta, elapsed], [total])
	frame_dig 7 // delta: uint64
	frame_dig 3 // elapsed: uint64
	mulw
	intc 0 // 0
	frame_dig 4 // total: uint64
	divmodw
	pop
	pop
	swap
	!

	// wideRatio failed
	assert
	frame_bury 8 // offset: uint64

	// contracts/AssetVault.algo.ts:646
	// return w1 > w0 ? w0 + offset : w0 - offset;
	frame_dig 6 // w1: uint64
	frame_dig 5 // w0: uint64
	>
	bz *ternary2_false
	frame_dig 5 // w0: uint64
	frame_dig 8 // offset: uint64
	+
	b *ternary2_end

*ternary2_false:
	frame_dig 5 // w0: uint64
	frame_dig 8 // offset: uint64
	-

*ternary2_end:

*getCurrentWeight*return:
	// set the subroutine return value
	frame_bury 0

	// pop all local variables from the stack
	popn 8
	retsub

// getTimes()uint64[]
*abi_route_getTimes:
	// The ABI return prefix
	bytec 6 // 0x151f7c75

	// execute getTimes()uint64[]
	callsub getTimes
	dup
	len
	intc 1 // 8
	/
	itob
	extract 6 2
	swap
	concat
	concat
	log
	intc 2 // 1
	return

// getTimes(): uint64[]
getTimes:
	proto 0 1

	// contracts/AssetVault.algo.ts:651
	// return [this.startRound.value, this.endRound.value, globals.round];
	bytec 8 //  "start_round"
	app_global_get
	itob
	bytec 7 //  "end_round"
	app_global_get
	itob
	concat
	global Round
	itob
	concat
	retsub

// opUp()void
*abi_route_opUp:
	// execute opUp()void
	callsub opUp
	intc 2 // 1
	return

// opUp(): void
opUp:
	proto 0 0
	retsub

*create_NoOp:
	txn NumAppArgs
	bz *abi_route_createApplication
	pushbytes 0xb8447b36 // method "createApplication()void"
	txna ApplicationArgs 0
	match *abi_route_createApplication

	// this contract does not implement the given ABI method for create NoOp
	err

*call_NoOp:
	pushbytes 0xdff25cce // method "prepare(address)void"
	pushbytes 0xd897f77d // method "addAssets(uint64[],uint64[])void"
	pushbytes 0x89279028 // method "finalizeBootstrap()uint64"
	pushbytes 0xafbb1f60 // method "addLiquidity(uint64,axfer)void"
	pushbytes 0xb2636155 // method "getLiquidity()uint64"
	pushbytes 0xc5db9b2d // method "burnLiquidity(axfer)void"
	pushbytes 0x8a37d55e // method "swap(uint64,uint64,uint64,axfer)uint64"
	pushbytes 0xf47ba5ea // method "changeWeights(uint64,uint64[])uint64"
	pushbytes 0x7194259e // method "getTotalAssets()uint64"
	pushbytes 0x20eb5394 // method "getToken()uint64"
	pushbytes 0x835bd9ea // method "getWeight(uint64)uint64"
	pushbytes 0x41310aee // method "getBalance(uint64)uint64"
	pushbytes 0x29200583 // method "estimateSwap(uint64,uint64,uint64)uint64"
	pushbytes 0x287889e2 // method "getCurrentWeight(uint64)uint64"
	pushbytes 0x89915be2 // method "getTimes()uint64[]"
	pushbytes 0x854dede0 // method "opUp()void"
	txna ApplicationArgs 0
	match *abi_route_prepare *abi_route_addAssets *abi_route_finalizeBootstrap *abi_route_addLiquidity *abi_route_getLiquidity *abi_route_burnLiquidity *abi_route_swap *abi_route_changeWeights *abi_route_getTotalAssets *abi_route_getToken *abi_route_getWeight *abi_route_getBalance *abi_route_estimateSwap *abi_route_getCurrentWeight *abi_route_getTimes *abi_route_opUp

	// this contract does not implement the given ABI method for call NoOp
	err

*intToAscii:
	proto 1 1
	pushbytes 0x30313233343536373839 // "0123456789"
	frame_dig -1 // i: uint64
	intc 2 // 1
	extract3
	retsub
	

*itoa:
	proto 1 1
	frame_dig -1 // i: uint64
	intc 0 // 0
	==
	bz *itoa_if_end
	pushbytes 0x30
	retsub

*itoa_if_end:
	frame_dig -1 // i: uint64
	intc 5 // 10
	/
	intc 0 // 0
	>
	bz *itoa_ternary_false
	frame_dig -1 // i: uint64
	intc 5 // 10
	/
	callsub *itoa
	b *itoa_ternary_end

*itoa_ternary_false:
	bytec 0 //  ""

*itoa_ternary_end:
	frame_dig -1 // i: uint64
	intc 5 // 10
	%
	callsub *intToAscii
	concat
	retsub