#pragma version 10
intcblock 0 8 1 1000000 10 6 4 32 2 5
bytecblock 0x 0x62616c616e6365735f 0x617373657473 0x70726f76696465645f 0x151f7c75 0x746f6b656e 0x656e645f74696d65 0x776569676874735f 0x73746172745f74696d65 0x7461726765745f776569676874735f 0x6275726e6564 0x0a8101 0x0000000000000000 0x6d616e61676572

// This TEAL was generated by TEALScript v0.106.2
// https://github.com/algorandfoundation/TEALScript

// This contract is compliant with and/or implements the following ARCs: [ ARC4 ]

// The following ten lines of TEAL handle initial program flow
// This pattern is used to make it easy for anyone to parse the start of the program and determine if a specific action is allowed
// Here, action refers to the OnComplete in combination with whether the app is being created or called
// Every possible action for this contract is represented in the switch statement
// If the action is not implemented in the contract, its respective branch will be "*NOT_IMPLEMENTED" which just contains "err"
txn ApplicationID
!
intc 5 // 6
*
txn OnCompletion
+
switch *call_NoOp *NOT_IMPLEMENTED *NOT_IMPLEMENTED *NOT_IMPLEMENTED *NOT_IMPLEMENTED *NOT_IMPLEMENTED *create_NoOp *NOT_IMPLEMENTED *NOT_IMPLEMENTED *NOT_IMPLEMENTED *NOT_IMPLEMENTED *NOT_IMPLEMENTED

*NOT_IMPLEMENTED:
	// The requested action is not implemented in this contract. Are you using the correct OnComplete? Did you set your app ID?
	err

// createApplication()void
*abi_route_createApplication:
	// execute createApplication()void
	callsub createApplication
	intc 2 // 1
	return

// createApplication(): void
//
// Initializes global state variables when the application is first created.
//
// This method is automatically invoked during the application's creation call (`NoOp` with bare create).
// It sets the initial manager to the app creator.
//
// This function should only be called once at contract deployment.
createApplication:
	proto 0 0

	// contracts/BalancedPoolV2.algo.ts:38
	// this.manager.value = this.app.creator
	bytec 13 //  "manager"
	txna Applications 0
	app_params_get AppCreator
	pop
	app_global_put

	// contracts/BalancedPoolV2.algo.ts:40
	// this.startTime.value = 0
	bytec 8 //  "start_time"
	intc 0 // 0
	app_global_put

	// contracts/BalancedPoolV2.algo.ts:41
	// this.endTime.value = 0
	bytec 6 //  "end_time"
	intc 0 // 0
	app_global_put
	retsub

// bootstrap(uint64[],uint64[])uint64
*abi_route_bootstrap:
	// The ABI return prefix
	bytec 4 // 0x151f7c75

	// weights: uint64[]
	txna ApplicationArgs 2
	extract 2 0

	// assetIds: uint64[]
	txna ApplicationArgs 1
	extract 2 0

	// execute bootstrap(uint64[],uint64[])uint64
	callsub bootstrap
	itob
	concat
	log
	intc 2 // 1
	return

// bootstrap(assetIds: AssetID[], weights: uint64[]): AssetID
//
// Bootstrap the pool by assigning assets and weights, create the LP tokens.
// @param {AssetID[]} assetIds - assets of the pool
// @param {uint64[]} weights - weights of the pool
// @return uint64 - LP Token created ID
bootstrap:
	proto 2 1

	// Push empty bytes after the frame pointer to reserve space for local variables
	bytec 0 // 0x
	dup

	// contracts/BalancedPoolV2.algo.ts:51
	// this.assertIsManager()
	callsub assertIsManager

	// contracts/BalancedPoolV2.algo.ts:52
	// assert(assetIds.length >= 2, 'At least 2 tokens needed')
	frame_dig -1 // assetIds: AssetID[]
	len
	intc 1 // 8
	/
	intc 8 // 2
	>=

	// At least 2 tokens needed
	assert

	// contracts/BalancedPoolV2.algo.ts:53
	// assert(assetIds.length === weights.length, 'Weights and Assets length must be the same')
	frame_dig -1 // assetIds: AssetID[]
	len
	intc 1 // 8
	/
	frame_dig -2 // weights: uint64[]
	len
	intc 1 // 8
	/
	==

	// Weights and Assets length must be the same
	assert

	// contracts/BalancedPoolV2.algo.ts:54
	// sumOfWeights = 0
	intc 0 // 0
	frame_bury 0 // sumOfWeights: uint64

	// contracts/BalancedPoolV2.algo.ts:56
	// for (let i = 0; i < assetIds.length; i += 1)
	intc 0 // 0
	frame_bury 1 // i: uint64

*for_0:
	// contracts/BalancedPoolV2.algo.ts:56
	// i < assetIds.length
	frame_dig 1 // i: uint64
	frame_dig -1 // assetIds: AssetID[]
	len
	intc 1 // 8
	/
	<
	bz *for_0_end

	// contracts/BalancedPoolV2.algo.ts:57
	// this.optIn(assetIds[i])
	frame_dig -1 // assetIds: AssetID[]
	store 255 // full array
	intc 0 //  initial offset
	frame_dig 1 // i: uint64
	intc 1 // 8
	* // acc * typeLength
	+
	load 255 // full array
	swap
	intc 1 // 8
	extract3
	btoi
	callsub optIn

	// contracts/BalancedPoolV2.algo.ts:58
	// this.addToken(i, assetIds[i], weights[i])
	frame_dig -2 // weights: uint64[]
	store 255 // full array
	intc 0 //  initial offset
	frame_dig 1 // i: uint64
	intc 1 // 8
	* // acc * typeLength
	+
	load 255 // full array
	swap
	intc 1 // 8
	extract3
	btoi
	frame_dig -1 // assetIds: AssetID[]
	store 255 // full array
	intc 0 //  initial offset
	frame_dig 1 // i: uint64
	intc 1 // 8
	* // acc * typeLength
	+
	load 255 // full array
	swap
	intc 1 // 8
	extract3
	btoi
	frame_dig 1 // i: uint64
	callsub addToken

	// contracts/BalancedPoolV2.algo.ts:59
	// sumOfWeights += weights[i]
	frame_dig 0 // sumOfWeights: uint64
	frame_dig -2 // weights: uint64[]
	store 255 // full array
	intc 0 //  initial offset
	frame_dig 1 // i: uint64
	intc 1 // 8
	* // acc * typeLength
	+
	load 255 // full array
	swap
	intc 1 // 8
	extract3
	btoi
	+
	frame_bury 0 // sumOfWeights: uint64

*for_0_continue:
	// contracts/BalancedPoolV2.algo.ts:56
	// i += 1
	frame_dig 1 // i: uint64
	intc 2 // 1
	+
	frame_bury 1 // i: uint64
	b *for_0

*for_0_end:
	// contracts/BalancedPoolV2.algo.ts:62
	// this.burned.value = 0
	bytec 10 //  "burned"
	intc 0 // 0
	app_global_put

	// contracts/BalancedPoolV2.algo.ts:63
	// this.assets.value = assetIds
	bytec 2 //  "assets"
	frame_dig -1 // assetIds: AssetID[]
	dup
	len
	intc 1 // 8
	/
	itob
	extract 6 2
	swap
	concat
	app_global_put

	// contracts/BalancedPoolV2.algo.ts:65
	// assert(this.absDiff(sumOfWeights, SCALE) <= 1, 'Weights must sum to 1')
	intc 3 // 1000000
	frame_dig 0 // sumOfWeights: uint64
	callsub absDiff
	intc 2 // 1
	<=

	// Weights must sum to 1
	assert

	// contracts/BalancedPoolV2.algo.ts:66
	// this.createToken()
	callsub createToken

	// contracts/BalancedPoolV2.algo.ts:68
	// return this.token.value;
	bytec 5 //  "token"
	app_global_get

	// set the subroutine return value
	frame_bury 0

	// pop all local variables from the stack
	popn 1
	retsub

// addLiquidity(uint64,uint64,address)void
*abi_route_addLiquidity:
	// sender: address
	txna ApplicationArgs 3
	dup
	len
	intc 7 // 32
	==

	// argument 0 (sender) for addLiquidity must be a address
	assert

	// amount: uint64
	txna ApplicationArgs 2
	btoi

	// index: uint64
	txna ApplicationArgs 1
	btoi

	// execute addLiquidity(uint64,uint64,address)void
	callsub addLiquidity
	intc 2 // 1
	return

// addLiquidity(index: uint64, amount: uint64, sender: Address): void
//
// Provide one token liquidity to the pool
// @param {uint64} index - index of the token in the pool
// @param {uint64} amount - amount of token sent
// @param {Address} sender - the sender
addLiquidity:
	proto 3 0

	// Push empty bytes after the frame pointer to reserve space for local variables
	bytec 0 // 0x

	// contracts/BalancedPoolV2.algo.ts:78
	// this.assertIsManager()
	callsub assertIsManager

	// contracts/BalancedPoolV2.algo.ts:79
	// this.assertIsBootstrapped()
	callsub assertIsBootstrapped

	// contracts/BalancedPoolV2.algo.ts:81
	// assetId = this.assets.value[index]
	bytec 2 //  "assets"
	app_global_get
	extract 2 0
	store 255 // full array
	intc 0 //  initial offset
	frame_dig -1 // index: uint64
	intc 1 // 8
	* // acc * typeLength
	+
	load 255 // full array
	swap
	intc 1 // 8
	extract3
	btoi
	frame_bury 0 // assetId: uint64

	// contracts/BalancedPoolV2.algo.ts:82
	// log('Asset ID => ' + itob(assetId))
	pushbytes 0x4173736574204944203d3e20 // "Asset ID => "
	frame_dig 0 // assetId: uint64
	itob
	concat
	log

	// contracts/BalancedPoolV2.algo.ts:84
	// this.optIn(assetId)
	frame_dig 0 // assetId: uint64
	callsub optIn

	// contracts/BalancedPoolV2.algo.ts:85
	// this.balances(assetId).value += amount
	bytec 1 //  "balances_"
	frame_dig 0 // assetId: uint64
	itob
	concat
	box_get

	// box value does not exist: this.balances(assetId).value
	assert
	btoi
	frame_dig -2 // amount: uint64
	+
	bytec 1 //  "balances_"
	frame_dig 0 // assetId: uint64
	itob
	concat
	swap
	itob
	box_put

	// *if0_condition
	// contracts/BalancedPoolV2.algo.ts:87
	// !this.provided(sender).exists
	bytec 3 //  "provided_"
	frame_dig -3 // sender: Address
	concat
	box_len
	swap
	pop
	!
	bz *if0_end

	// *if0_consequent
	// contracts/BalancedPoolV2.algo.ts:88
	// this.provided(sender).create((this.assets.value.length + 1) * 8)
	bytec 3 //  "provided_"
	frame_dig -3 // sender: Address
	concat
	bytec 2 //  "assets"
	app_global_get
	extract 2 0
	len
	intc 1 // 8
	/
	intc 2 // 1
	+
	intc 1 // 8
	*
	box_create
	pop

*if0_end:
	// contracts/BalancedPoolV2.algo.ts:91
	// this.provided(sender).value[index] += amount
	bytec 3 //  "provided_"
	frame_dig -3 // sender: Address
	concat
	box_get

	// box value does not exist: this.provided(sender).value
	assert
	extract 2 0
	store 255 // full array
	intc 0 //  initial offset
	frame_dig -1 // index: uint64
	intc 1 // 8
	* // acc * typeLength
	+
	load 255 // full array
	swap
	bytec 3 //  "provided_"
	frame_dig -3 // sender: Address
	concat
	box_get

	// box value does not exist: this.provided(sender).value
	assert
	extract 2 0
	store 255 // full array
	intc 0 //  initial offset
	frame_dig -1 // index: uint64
	intc 1 // 8
	* // acc * typeLength
	+
	load 255 // full array
	swap
	intc 1 // 8
	extract3
	btoi
	frame_dig -2 // amount: uint64
	+
	itob
	replace3
	bytec 3 //  "provided_"
	frame_dig -3 // sender: Address
	concat
	dup
	box_del
	pop
	swap
	dup
	len
	intc 1 // 8
	/
	itob
	extract 6 2
	swap
	concat
	box_put
	retsub

// getLiquidity(address)uint64
*abi_route_getLiquidity:
	// The ABI return prefix
	bytec 4 // 0x151f7c75

	// sender: address
	txna ApplicationArgs 1
	dup
	len
	intc 7 // 32
	==

	// argument 0 (sender) for getLiquidity must be a address
	assert

	// execute getLiquidity(address)uint64
	callsub getLiquidity
	itob
	concat
	log
	intc 2 // 1
	return

// getLiquidity(sender: Address): uint64
//
// Mints LP tokens to the given sender based on the liquidity they provided.
//
// If this is the first liquidity provision (i.e., total LP supply is 0),
// a fixed initial amount is minted to the sender. Otherwise, the amount
// is calculated proportionally using `computeNAssetsLiquidity()`.
//
// After minting, the sender's "provided" state is reset.
//
// @param sender - The address receiving the LP tokens
// @returns The amount of LP tokens minted
getLiquidity:
	proto 1 1

	// Push empty bytes after the frame pointer to reserve space for local variables
	bytec 0 // 0x
	dup

	// contracts/BalancedPoolV2.algo.ts:107
	// this.assertIsManager()
	callsub assertIsManager

	// contracts/BalancedPoolV2.algo.ts:108
	// this.assertIsBootstrapped()
	callsub assertIsBootstrapped

	// contracts/BalancedPoolV2.algo.ts:110
	// amount: uint64 = 0
	intc 0 // 0
	frame_bury 0 // amount: uint64

	// *if1_condition
	// contracts/BalancedPoolV2.algo.ts:112
	// this.totalLP() === 0
	callsub totalLP
	intc 0 // 0
	==
	bz *if1_else

	// *if1_consequent
	// contracts/BalancedPoolV2.algo.ts:114
	// amount = AMOUNT_LP_DEPLOYER
	pushint 1_000_000
	intc 4 // 10
	intc 5 // 6
	exp
	*
	frame_bury 0 // amount: uint64
	b *if1_end

*if1_else:
	// contracts/BalancedPoolV2.algo.ts:116
	// amount = this.computeNAssetsLiquidity(sender)
	frame_dig -1 // sender: Address
	callsub computeNAssetsLiquidity
	frame_bury 0 // amount: uint64

*if1_end:
	// contracts/BalancedPoolV2.algo.ts:119
	// for (let i = 0; i < this.provided(sender).value.length; i += 1)
	intc 0 // 0
	frame_bury 1 // i: uint64

*for_1:
	// contracts/BalancedPoolV2.algo.ts:119
	// i < this.provided(sender).value.length
	frame_dig 1 // i: uint64
	bytec 3 //  "provided_"
	frame_dig -1 // sender: Address
	concat
	box_get

	// box value does not exist: this.provided(sender).value
	assert
	extract 2 0
	len
	intc 1 // 8
	/
	<
	bz *for_1_end

	// contracts/BalancedPoolV2.algo.ts:120
	// this.provided(sender).value[i] = 0
	bytec 3 //  "provided_"
	frame_dig -1 // sender: Address
	concat
	box_get

	// box value does not exist: this.provided(sender).value
	assert
	extract 2 0
	store 255 // full array
	intc 0 //  initial offset
	frame_dig 1 // i: uint64
	intc 1 // 8
	* // acc * typeLength
	+
	load 255 // full array
	swap
	bytec 12 // 0x0000000000000000
	replace3
	bytec 3 //  "provided_"
	frame_dig -1 // sender: Address
	concat
	dup
	box_del
	pop
	swap
	dup
	len
	intc 1 // 8
	/
	itob
	extract 6 2
	swap
	concat
	box_put

*for_1_continue:
	// contracts/BalancedPoolV2.algo.ts:119
	// i += 1
	frame_dig 1 // i: uint64
	intc 2 // 1
	+
	frame_bury 1 // i: uint64
	b *for_1

*for_1_end:
	// contracts/BalancedPoolV2.algo.ts:123
	// sendAssetTransfer({
	//       assetReceiver: sender,
	//       assetAmount: amount,
	//       xferAsset: this.token.value,
	//     })
	itxn_begin
	intc 6 //  axfer
	itxn_field TypeEnum

	// contracts/BalancedPoolV2.algo.ts:124
	// assetReceiver: sender
	frame_dig -1 // sender: Address
	itxn_field AssetReceiver

	// contracts/BalancedPoolV2.algo.ts:125
	// assetAmount: amount
	frame_dig 0 // amount: uint64
	itxn_field AssetAmount

	// contracts/BalancedPoolV2.algo.ts:126
	// xferAsset: this.token.value
	bytec 5 //  "token"
	app_global_get
	itxn_field XferAsset

	// Fee field not set, defaulting to 0
	intc 0 // 0
	itxn_field Fee

	// Submit inner transaction
	itxn_submit

	// contracts/BalancedPoolV2.algo.ts:129
	// return amount;
	frame_dig 0 // amount: uint64

	// set the subroutine return value
	frame_bury 0

	// pop all local variables from the stack
	popn 1
	retsub

// burnLiquidity(address,uint64)void
*abi_route_burnLiquidity:
	// amountLP: uint64
	txna ApplicationArgs 2
	btoi

	// sender: address
	txna ApplicationArgs 1
	dup
	len
	intc 7 // 32
	==

	// argument 1 (sender) for burnLiquidity must be a address
	assert

	// execute burnLiquidity(address,uint64)void
	callsub burnLiquidity
	intc 2 // 1
	return

// burnLiquidity(sender: Address, amountLP: uint64): void
//
// Burns a given amount of LP tokens from the sender and returns
// their proportional share of each asset in the pool.
//
// The withdrawn amount for each asset is calculated based on the
// ratio of `amountLP` to the total LP supply.
//
// @param sender - The address burning LP tokens
// @param amountLP - The amount of LP tokens to burn
burnLiquidity:
	proto 2 0

	// Push empty bytes after the frame pointer to reserve space for local variables
	bytec 0 // 0x
	dupn 5

	// contracts/BalancedPoolV2.algo.ts:143
	// this.assertIsManager()
	callsub assertIsManager

	// contracts/BalancedPoolV2.algo.ts:144
	// this.assertIsBootstrapped()
	callsub assertIsBootstrapped

	// contracts/BalancedPoolV2.algo.ts:145
	// assert(amountLP > 0, 'Must burn positive amount')
	frame_dig -2 // amountLP: uint64
	intc 0 // 0
	>

	// Must burn positive amount
	assert

	// contracts/BalancedPoolV2.algo.ts:147
	// totalLP = this.totalLP()
	callsub totalLP
	frame_bury 0 // totalLP: uint64

	// contracts/BalancedPoolV2.algo.ts:148
	// numAssets = this.assets.value.length
	bytec 2 //  "assets"
	app_global_get
	extract 2 0
	len
	intc 1 // 8
	/
	frame_bury 1 // numAssets: uint64

	// contracts/BalancedPoolV2.algo.ts:150
	// for (let i = 0; i < numAssets; i += 1)
	intc 0 // 0
	frame_bury 2 // i: uint64

*for_2:
	// contracts/BalancedPoolV2.algo.ts:150
	// i < numAssets
	frame_dig 2 // i: uint64
	frame_dig 1 // numAssets: uint64
	<
	bz *for_2_end

	// contracts/BalancedPoolV2.algo.ts:151
	// assetId = this.assets.value[i]
	bytec 2 //  "assets"
	app_global_get
	extract 2 0
	store 255 // full array
	intc 0 //  initial offset
	frame_dig 2 // i: uint64
	intc 1 // 8
	* // acc * typeLength
	+
	load 255 // full array
	swap
	intc 1 // 8
	extract3
	btoi
	frame_bury 3 // assetId: uint64

	// contracts/BalancedPoolV2.algo.ts:152
	// poolBalance = this.balances(assetId).value
	bytec 1 //  "balances_"
	frame_dig 3 // assetId: uint64
	itob
	concat
	box_get

	// box value does not exist: this.balances(assetId).value
	assert
	btoi
	frame_bury 4 // poolBalance: uint64

	// contracts/BalancedPoolV2.algo.ts:154
	// assetAmount = wideRatio([amountLP, poolBalance], [totalLP])
	frame_dig -2 // amountLP: uint64
	frame_dig 4 // poolBalance: uint64
	mulw
	intc 0 // 0
	frame_dig 0 // totalLP: uint64
	divmodw
	pop
	pop
	swap
	!

	// wideRatio failed
	assert
	frame_bury 5 // assetAmount: uint64

	// contracts/BalancedPoolV2.algo.ts:156
	// this.balances(assetId).value = poolBalance - assetAmount
	bytec 1 //  "balances_"
	frame_dig 3 // assetId: uint64
	itob
	concat
	frame_dig 4 // poolBalance: uint64
	frame_dig 5 // assetAmount: uint64
	-
	itob
	box_put

	// contracts/BalancedPoolV2.algo.ts:158
	// sendAssetTransfer({
	//         assetReceiver: sender,
	//         assetAmount: assetAmount,
	//         xferAsset: assetId,
	//       })
	itxn_begin
	intc 6 //  axfer
	itxn_field TypeEnum

	// contracts/BalancedPoolV2.algo.ts:159
	// assetReceiver: sender
	frame_dig -1 // sender: Address
	itxn_field AssetReceiver

	// contracts/BalancedPoolV2.algo.ts:160
	// assetAmount: assetAmount
	frame_dig 5 // assetAmount: uint64
	itxn_field AssetAmount

	// contracts/BalancedPoolV2.algo.ts:161
	// xferAsset: assetId
	frame_dig 3 // assetId: uint64
	itxn_field XferAsset

	// Fee field not set, defaulting to 0
	intc 0 // 0
	itxn_field Fee

	// Submit inner transaction
	itxn_submit

*for_2_continue:
	// contracts/BalancedPoolV2.algo.ts:150
	// i += 1
	frame_dig 2 // i: uint64
	intc 2 // 1
	+
	frame_bury 2 // i: uint64
	b *for_2

*for_2_end:
	// contracts/BalancedPoolV2.algo.ts:165
	// this.burned.value += amountLP
	bytec 10 //  "burned"
	app_global_get
	frame_dig -2 // amountLP: uint64
	+
	bytec 10 //  "burned"
	swap
	app_global_put
	retsub

// swap(address,uint64,uint64,uint64)uint64
*abi_route_swap:
	// The ABI return prefix
	bytec 4 // 0x151f7c75

	// amount: uint64
	txna ApplicationArgs 4
	btoi

	// to: uint64
	txna ApplicationArgs 3
	btoi

	// from: uint64
	txna ApplicationArgs 2
	btoi

	// sender: address
	txna ApplicationArgs 1
	dup
	len
	intc 7 // 32
	==

	// argument 3 (sender) for swap must be a address
	assert

	// execute swap(address,uint64,uint64,uint64)uint64
	callsub swap
	itob
	concat
	log
	intc 2 // 1
	return

// swap(sender: Address, from: uint64, to: uint64, amount: uint64): uint64
//
// Executes a weighted swap between two tokens in the pool based on the constant mean formula.
//
// The input token (`from`) is sent into the pool, and the output token (`to`) is sent back
// to the sender, following the AMM's pricing curve determined by current balances and weights.
//
// This function performs the following steps:
// - Retrieves the current weights and balances for the two assets.
// - Calculates the output amount using the invariant pricing function (`calcOut`).
// - Updates the pool's internal balances accordingly.
// - Transfers the output asset to the sender.
//
// @param sender - The address initiating the swap.
// @param from - Index of the input asset in the pool.
// @param to - Index of the output asset in the pool.
// @param amount - Amount of input asset to swap.
// @returns The amount of output asset received.
swap:
	proto 4 1

	// Push empty bytes after the frame pointer to reserve space for local variables
	bytec 0 // 0x
	dupn 6

	// contracts/BalancedPoolV2.algo.ts:187
	// this.assertIsManager()
	callsub assertIsManager

	// contracts/BalancedPoolV2.algo.ts:188
	// this.assertIsBootstrapped()
	callsub assertIsBootstrapped

	// contracts/BalancedPoolV2.algo.ts:190
	// assetIn = this.assets.value[from]
	bytec 2 //  "assets"
	app_global_get
	extract 2 0
	store 255 // full array
	intc 0 //  initial offset
	frame_dig -2 // from: uint64
	intc 1 // 8
	* // acc * typeLength
	+
	load 255 // full array
	swap
	intc 1 // 8
	extract3
	btoi
	frame_bury 0 // assetIn: uint64

	// contracts/BalancedPoolV2.algo.ts:191
	// assetOut = this.assets.value[to]
	bytec 2 //  "assets"
	app_global_get
	extract 2 0
	store 255 // full array
	intc 0 //  initial offset
	frame_dig -3 // to: uint64
	intc 1 // 8
	* // acc * typeLength
	+
	load 255 // full array
	swap
	intc 1 // 8
	extract3
	btoi
	frame_bury 1 // assetOut: uint64

	// contracts/BalancedPoolV2.algo.ts:193
	// balanceIn = this.balances(assetIn).value
	bytec 1 //  "balances_"
	frame_dig 0 // assetIn: uint64
	itob
	concat
	box_get

	// box value does not exist: this.balances(assetIn).value
	assert
	btoi
	frame_bury 2 // balanceIn: uint64

	// contracts/BalancedPoolV2.algo.ts:194
	// balanceOut = this.balances(assetOut).value
	bytec 1 //  "balances_"
	frame_dig 1 // assetOut: uint64
	itob
	concat
	box_get

	// box value does not exist: this.balances(assetOut).value
	assert
	btoi
	frame_bury 3 // balanceOut: uint64

	// contracts/BalancedPoolV2.algo.ts:196
	// weightIn = this.getCurrentWeight(from)
	frame_dig -2 // from: uint64
	callsub getCurrentWeight
	frame_bury 4 // weightIn: uint64

	// contracts/BalancedPoolV2.algo.ts:197
	// weightOut = this.getCurrentWeight(to)
	frame_dig -3 // to: uint64
	callsub getCurrentWeight
	frame_bury 5 // weightOut: uint64

	// contracts/BalancedPoolV2.algo.ts:199
	// amountOut = this.calcOut(balanceIn, weightIn, balanceOut, weightOut, amount)
	frame_dig -4 // amount: uint64
	frame_dig 5 // weightOut: uint64
	frame_dig 3 // balanceOut: uint64
	frame_dig 4 // weightIn: uint64
	frame_dig 2 // balanceIn: uint64
	callsub calcOut
	frame_bury 6 // amountOut: uint64

	// contracts/BalancedPoolV2.algo.ts:201
	// log(itob(amountOut))
	frame_dig 6 // amountOut: uint64
	itob
	log

	// contracts/BalancedPoolV2.algo.ts:203
	// this.balances(assetIn).value = balanceIn + amount
	bytec 1 //  "balances_"
	frame_dig 0 // assetIn: uint64
	itob
	concat
	frame_dig 2 // balanceIn: uint64
	frame_dig -4 // amount: uint64
	+
	itob
	box_put

	// contracts/BalancedPoolV2.algo.ts:204
	// this.balances(assetOut).value = balanceOut - amountOut
	bytec 1 //  "balances_"
	frame_dig 1 // assetOut: uint64
	itob
	concat
	frame_dig 3 // balanceOut: uint64
	frame_dig 6 // amountOut: uint64
	-
	itob
	box_put

	// contracts/BalancedPoolV2.algo.ts:206
	// sendAssetTransfer({
	//       assetReceiver: sender,
	//       assetAmount: amountOut,
	//       xferAsset: assetOut,
	//     })
	itxn_begin
	intc 6 //  axfer
	itxn_field TypeEnum

	// contracts/BalancedPoolV2.algo.ts:207
	// assetReceiver: sender
	frame_dig -1 // sender: Address
	itxn_field AssetReceiver

	// contracts/BalancedPoolV2.algo.ts:208
	// assetAmount: amountOut
	frame_dig 6 // amountOut: uint64
	itxn_field AssetAmount

	// contracts/BalancedPoolV2.algo.ts:209
	// xferAsset: assetOut
	frame_dig 1 // assetOut: uint64
	itxn_field XferAsset

	// Fee field not set, defaulting to 0
	intc 0 // 0
	itxn_field Fee

	// Submit inner transaction
	itxn_submit

	// contracts/BalancedPoolV2.algo.ts:212
	// return amountOut;
	frame_dig 6 // amountOut: uint64

	// set the subroutine return value
	frame_bury 0

	// pop all local variables from the stack
	popn 6
	retsub

// changeWeights(uint64,uint64[])uint64
*abi_route_changeWeights:
	// The ABI return prefix
	bytec 4 // 0x151f7c75

	// newWeights: uint64[]
	txna ApplicationArgs 2
	extract 2 0

	// duration: uint64
	txna ApplicationArgs 1
	btoi

	// execute changeWeights(uint64,uint64[])uint64
	callsub changeWeights
	itob
	concat
	log
	intc 2 // 1
	return

// changeWeights(duration: uint64, newWeights: uint64[]): uint64
//
// Updates the pool's asset weights, either immediately or with a time-based linear interpolation.
//
// If `duration` is zero, the new weights are applied immediately by overwriting the current weights.
// Otherwise, a linear transition is initiated from the current weights to `newWeights` over the specified
// duration (measured in seconds or microseconds (?)).
//
// During the transition period, weights are dynamically computed based on the elapsed time
// between `startTime` and `endTime`, and stored in `targetWeights`. The current weights must be
// retrieved using a function like `getCurrentWeight()` for accurate interpolated values.
//
// @param {uint64[]} newWeights - Array of new target weights for each asset in the pool.
// @param {uint64} duration - Duration of the interpolation. If 0, the weights are updated instantly.
changeWeights:
	proto 2 1

	// Push empty bytes after the frame pointer to reserve space for local variables
	bytec 0 // 0x
	dup

	// contracts/BalancedPoolV2.algo.ts:230
	// this.assertIsManager()
	callsub assertIsManager

	// contracts/BalancedPoolV2.algo.ts:231
	// this.assertIsBootstrapped()
	callsub assertIsBootstrapped

	// contracts/BalancedPoolV2.algo.ts:233
	// currentTime = globals.latestTimestamp
	global LatestTimestamp
	frame_bury 0 // currentTime: uint64

	// contracts/BalancedPoolV2.algo.ts:235
	// this.startTime.value = currentTime
	bytec 8 //  "start_time"
	frame_dig 0 // currentTime: uint64
	app_global_put

	// contracts/BalancedPoolV2.algo.ts:236
	// this.endTime.value = currentTime + duration
	bytec 6 //  "end_time"
	frame_dig 0 // currentTime: uint64
	frame_dig -1 // duration: uint64
	+
	app_global_put

	// contracts/BalancedPoolV2.algo.ts:238
	// for (let i = 0; i < newWeights.length; i += 1)
	intc 0 // 0
	frame_bury 1 // i: uint64

*for_3:
	// contracts/BalancedPoolV2.algo.ts:238
	// i < newWeights.length
	frame_dig 1 // i: uint64
	frame_dig -2 // newWeights: uint64[]
	len
	intc 1 // 8
	/
	<
	bz *for_3_end

	// contracts/BalancedPoolV2.algo.ts:239
	// this.targetWeights(i).value = newWeights[i]
	bytec 9 //  "target_weights_"
	frame_dig 1 // i: uint64
	itob
	concat
	frame_dig -2 // newWeights: uint64[]
	store 255 // full array
	intc 0 //  initial offset
	frame_dig 1 // i: uint64
	intc 1 // 8
	* // acc * typeLength
	+
	load 255 // full array
	swap
	intc 1 // 8
	extract3
	btoi
	itob
	box_put

*for_3_continue:
	// contracts/BalancedPoolV2.algo.ts:238
	// i += 1
	frame_dig 1 // i: uint64
	intc 2 // 1
	+
	frame_bury 1 // i: uint64
	b *for_3

*for_3_end:
	// contracts/BalancedPoolV2.algo.ts:242
	// return this.endTime.value;
	bytec 6 //  "end_time"
	app_global_get

	// set the subroutine return value
	frame_bury 0

	// pop all local variables from the stack
	popn 1
	retsub

// finalizeWeights(): void
finalizeWeights:
	proto 0 0

	// Push empty bytes after the frame pointer to reserve space for local variables
	bytec 0 // 0x

	// *if2_condition
	// contracts/BalancedPoolV2.algo.ts:246
	// globals.latestTimestamp >= this.endTime.value
	global LatestTimestamp
	bytec 6 //  "end_time"
	app_global_get
	>=
	bz *if2_end

	// *if2_consequent
	// contracts/BalancedPoolV2.algo.ts:247
	// for (let i = 0; i < this.assets.value.length; i += 1)
	intc 0 // 0
	frame_bury 0 // i: uint64

*for_4:
	// contracts/BalancedPoolV2.algo.ts:247
	// i < this.assets.value.length
	frame_dig 0 // i: uint64
	bytec 2 //  "assets"
	app_global_get
	extract 2 0
	len
	intc 1 // 8
	/
	<
	bz *for_4_end

	// contracts/BalancedPoolV2.algo.ts:248
	// this.weights(i).value = this.targetWeights(i).value
	bytec 7 //  "weights_"
	frame_dig 0 // i: uint64
	itob
	concat
	bytec 9 //  "target_weights_"
	frame_dig 0 // i: uint64
	itob
	concat
	box_get

	// box value does not exist: this.targetWeights(i).value
	assert
	btoi
	itob
	box_put

*for_4_continue:
	// contracts/BalancedPoolV2.algo.ts:247
	// i += 1
	frame_dig 0 // i: uint64
	intc 2 // 1
	+
	frame_bury 0 // i: uint64
	b *for_4

*for_4_end:
	// contracts/BalancedPoolV2.algo.ts:250
	// this.startTime.value = 0
	bytec 8 //  "start_time"
	intc 0 // 0
	app_global_put

	// contracts/BalancedPoolV2.algo.ts:251
	// this.endTime.value = 0
	bytec 6 //  "end_time"
	intc 0 // 0
	app_global_put

*if2_end:
	retsub

// optIn(assetId: AssetID): void
//
// *******************
// SUBROUTINES
// *******************
//
// Opts the application into a given ASA if not already opted-in.
//
// @param assetId - The ID of the asset to opt into.
optIn:
	proto 1 0

	// *if3_condition
	// contracts/BalancedPoolV2.algo.ts:265
	// this.app.address.isOptedInToAsset(assetId)
	global CurrentApplicationAddress
	frame_dig -1 // assetId: AssetID
	asset_holding_get AssetBalance
	swap
	pop
	bz *if3_end

	// *if3_consequent
	// contracts/BalancedPoolV2.algo.ts:266
	// return;
	retsub

*if3_end:
	// contracts/BalancedPoolV2.algo.ts:269
	// sendAssetTransfer({
	//       assetReceiver: this.app.address,
	//       xferAsset: assetId,
	//       assetAmount: 0,
	//     })
	itxn_begin
	intc 6 //  axfer
	itxn_field TypeEnum

	// contracts/BalancedPoolV2.algo.ts:270
	// assetReceiver: this.app.address
	global CurrentApplicationAddress
	itxn_field AssetReceiver

	// contracts/BalancedPoolV2.algo.ts:271
	// xferAsset: assetId
	frame_dig -1 // assetId: AssetID
	itxn_field XferAsset

	// contracts/BalancedPoolV2.algo.ts:272
	// assetAmount: 0
	intc 0 // 0
	itxn_field AssetAmount

	// Fee field not set, defaulting to 0
	intc 0 // 0
	itxn_field Fee

	// Submit inner transaction
	itxn_submit
	retsub

// addToken(index: uint64, assetID: AssetID, weight: uint64): void
//
// Registers a new token in the pool by initializing its balance and weight.
//
// This function creates and sets:
// - A balance box for the token, initialized to 0.
// - A weight box for the token's index, used in weighted operations like swaps.
//
// It assumes the caller has already validated inputs and manages order/indexing externally.
//
// @param index - Index of the token within the pool.
// @param assetID - The ASA ID of the token to add.
// @param weight - The normalized weight assigned to the token (e.g., scaled by 1e6).
addToken:
	proto 3 0

	// *if4_condition
	// contracts/BalancedPoolV2.algo.ts:290
	// !this.weights(index).exists
	bytec 7 //  "weights_"
	frame_dig -1 // index: uint64
	itob
	concat
	box_len
	swap
	pop
	!
	bz *if4_end

	// *if4_consequent
	// contracts/BalancedPoolV2.algo.ts:291
	// this.weights(index).create(8)
	bytec 7 //  "weights_"
	frame_dig -1 // index: uint64
	itob
	concat
	intc 1 // 8
	box_create
	pop

*if4_end:
	// *if5_condition
	// contracts/BalancedPoolV2.algo.ts:294
	// !this.balances(assetID).exists
	bytec 1 //  "balances_"
	frame_dig -2 // assetID: AssetID
	itob
	concat
	box_len
	swap
	pop
	!
	bz *if5_end

	// *if5_consequent
	// contracts/BalancedPoolV2.algo.ts:295
	// this.balances(assetID).create(8)
	bytec 1 //  "balances_"
	frame_dig -2 // assetID: AssetID
	itob
	concat
	intc 1 // 8
	box_create
	pop

*if5_end:
	// contracts/BalancedPoolV2.algo.ts:298
	// this.weights(index).value = weight
	bytec 7 //  "weights_"
	frame_dig -1 // index: uint64
	itob
	concat
	frame_dig -3 // weight: uint64
	itob
	box_put

	// contracts/BalancedPoolV2.algo.ts:299
	// this.balances(assetID).value = 0
	bytec 1 //  "balances_"
	frame_dig -2 // assetID: AssetID
	itob
	concat
	bytec 12 // 0x0000000000000000
	box_put
	retsub

// createToken(): void
//
// Creates the LP (liquidity provider) token for the pool if it does not already exist.
//
// The LP token is an Algorand Standard Asset (ASA) that represents a user's proportional
// share of the pool.
// This function ensures only one token is created, and sets the contract
// as its manager and reserve.
//
// The token is configured with:
// - Total supply: `TOTAL_LP_SUPPLY`
// - Decimals: 6
// - Reserve: this contract's address
// - No clawback/freeze addresses
// - Default frozen: false
//
// The Token name is dynamically derived from the application ID.
createToken:
	proto 0 0

	// *if6_condition
	// contracts/BalancedPoolV2.algo.ts:320
	// this.token.value === AssetID.zeroIndex
	bytec 5 //  "token"
	app_global_get
	intc 0 // 0
	==
	bz *if6_end

	// *if6_consequent
	// contracts/BalancedPoolV2.algo.ts:321
	// this.token.value = sendAssetCreation({
	//         configAssetTotal: TOTAL_LP_SUPPLY,
	//         configAssetDecimals: 6,
	//         configAssetReserve: this.app.address,
	//         configAssetManager: this.app.address,
	//         configAssetClawback: globals.zeroAddress,
	//         configAssetFreeze: globals.zeroAddress,
	//         configAssetDefaultFrozen: 0,
	//         configAssetName: 'BalancedPool-' + this.app.id.toString(),
	//         configAssetUnitName: 'LP',
	//       })
	bytec 5 //  "token"
	itxn_begin
	pushint 3 // acfg
	itxn_field TypeEnum

	// contracts/BalancedPoolV2.algo.ts:322
	// configAssetTotal: TOTAL_LP_SUPPLY
	intc 4 // 10
	pushint 16
	exp
	itxn_field ConfigAssetTotal

	// contracts/BalancedPoolV2.algo.ts:323
	// configAssetDecimals: 6
	intc 5 // 6
	itxn_field ConfigAssetDecimals

	// contracts/BalancedPoolV2.algo.ts:324
	// configAssetReserve: this.app.address
	global CurrentApplicationAddress
	itxn_field ConfigAssetReserve

	// contracts/BalancedPoolV2.algo.ts:325
	// configAssetManager: this.app.address
	global CurrentApplicationAddress
	itxn_field ConfigAssetManager

	// contracts/BalancedPoolV2.algo.ts:326
	// configAssetClawback: globals.zeroAddress
	global ZeroAddress
	itxn_field ConfigAssetClawback

	// contracts/BalancedPoolV2.algo.ts:327
	// configAssetFreeze: globals.zeroAddress
	global ZeroAddress
	itxn_field ConfigAssetFreeze

	// contracts/BalancedPoolV2.algo.ts:328
	// configAssetDefaultFrozen: 0
	intc 0 // 0
	itxn_field ConfigAssetDefaultFrozen

	// contracts/BalancedPoolV2.algo.ts:329
	// configAssetName: 'BalancedPool-' + this.app.id.toString()
	pushbytes 0x42616c616e636564506f6f6c2d // "BalancedPool-"
	txna Applications 0
	callsub *itoa
	concat
	itxn_field ConfigAssetName

	// contracts/BalancedPoolV2.algo.ts:330
	// configAssetUnitName: 'LP'
	pushbytes 0x4c50 // "LP"
	itxn_field ConfigAssetUnitName

	// Fee field not set, defaulting to 0
	intc 0 // 0
	itxn_field Fee

	// Submit inner transaction
	itxn_submit
	itxn CreatedAssetID
	app_global_put

*if6_end:
	retsub

// assertIsManager(): void
//
// Assert the tx sender is the manager
assertIsManager:
	proto 0 0

	// contracts/BalancedPoolV2.algo.ts:339
	// assert(this.txn.sender === this.manager.value, 'only the manager can call this method')
	txn Sender
	bytec 13 //  "manager"
	app_global_get
	==

	// only the manager can call this method
	assert
	retsub

// assertIsBootstrapped(): void
assertIsBootstrapped:
	proto 0 0

	// contracts/BalancedPoolV2.algo.ts:343
	// assert(this.token.value !== AssetID.zeroIndex, 'pool not bootstrapped')
	bytec 5 //  "token"
	app_global_get
	intc 0 // 0
	!=

	// pool not bootstrapped
	assert
	retsub

// lnWithSign(x: uint64): uint64[]
//
// Approximates the natural logarithm of a fixed-point value `x` with sign support.
//
// Uses a rational approximation of ln(x) via the Mercator series, centered around 1.
// If `x < SCALE`, the logarithm of the inverse is computed and a `negative` flag is returned.
// This is used to handle values less than 1 while keeping precision stable.
//
// @param x - Input value in fixed-point representation (scaled by SCALE).
// @returns A tuple [negative: uint64, result: uint64] where:
// - negative = 1 if log(x) is negative
// - result = absolute value of log(x) scaled by SCALE
lnWithSign:
	proto 1 1

	// Push empty bytes after the frame pointer to reserve space for local variables
	bytec 0 // 0x
	dupn 7

	// contracts/BalancedPoolV2.algo.ts:359
	// assert(x > 0, 'log undefined for x ≤ 0')
	frame_dig -1 // x: uint64
	intc 0 // 0
	>

	// log undefined for x ≤ 0
	assert

	// contracts/BalancedPoolV2.algo.ts:361
	// negative: uint64 = 0
	intc 0 // 0
	frame_bury 0 // negative: uint64

	// *if7_condition
	// contracts/BalancedPoolV2.algo.ts:364
	// x < SCALE
	frame_dig -1 // x: uint64
	intc 3 // 1000000
	<
	bz *if7_else

	// *if7_consequent
	// contracts/BalancedPoolV2.algo.ts:365
	// negative = 1
	intc 2 // 1
	frame_bury 0 // negative: uint64

	// contracts/BalancedPoolV2.algo.ts:366
	// invX = wideRatio([SCALE, SCALE], [x])
	intc 3 // 1000000
	dup
	mulw
	intc 0 // 0
	frame_dig -1 // x: uint64
	divmodw
	pop
	pop
	swap
	!

	// wideRatio failed
	assert
	frame_bury 2 // invX: uint64

	// contracts/BalancedPoolV2.algo.ts:367
	// z = wideRatio([invX - SCALE, SCALE], [invX])
	frame_dig 2 // invX: uint64
	intc 3 // 1000000
	-
	intc 3 // 1000000
	mulw
	intc 0 // 0
	frame_dig 2 // invX: uint64
	divmodw
	pop
	pop
	swap
	!

	// wideRatio failed
	assert
	frame_bury 1 // z: uint64
	b *if7_end

*if7_else:
	// contracts/BalancedPoolV2.algo.ts:369
	// z = wideRatio([x - SCALE, SCALE], [x])
	frame_dig -1 // x: uint64
	intc 3 // 1000000
	-
	intc 3 // 1000000
	mulw
	intc 0 // 0
	frame_dig -1 // x: uint64
	divmodw
	pop
	pop
	swap
	!

	// wideRatio failed
	assert
	frame_bury 1 // z: uint64

*if7_end:
	// contracts/BalancedPoolV2.algo.ts:372
	// result = z
	frame_dig 1 // z: uint64
	frame_bury 3 // result: uint64

	// contracts/BalancedPoolV2.algo.ts:373
	// term = z
	frame_dig 1 // z: uint64
	frame_bury 4 // term: uint64

	// contracts/BalancedPoolV2.algo.ts:374
	// neg = false
	intc 0 // 0
	frame_bury 5 // neg: bool

	// contracts/BalancedPoolV2.algo.ts:376
	// increaseOpcodeBudget()
	itxn_begin
	intc 5 //  appl
	itxn_field TypeEnum
	intc 0 // 0
	itxn_field Fee
	bytec 11 //  #pragma version 10; int 1
	dup
	itxn_field ApprovalProgram
	itxn_field ClearStateProgram
	intc 9 //  DeleteApplication
	itxn_field OnCompletion
	itxn_submit

	// contracts/BalancedPoolV2.algo.ts:378
	// for (let i = 2; i <= 10; i = i + 1)
	intc 8 // 2
	frame_bury 6 // i: uint64

*for_5:
	// contracts/BalancedPoolV2.algo.ts:378
	// i <= 10
	frame_dig 6 // i: uint64
	intc 4 // 10
	<=
	bz *for_5_end

	// contracts/BalancedPoolV2.algo.ts:379
	// term = wideRatio([term, z], [SCALE])
	frame_dig 4 // term: uint64
	frame_dig 1 // z: uint64
	mulw
	intc 0 // 0
	intc 3 // 1000000
	divmodw
	pop
	pop
	swap
	!

	// wideRatio failed
	assert
	frame_bury 4 // term: uint64

	// contracts/BalancedPoolV2.algo.ts:380
	// delta = wideRatio([term], [i])
	intc 0 // 0
	frame_dig 4 // term: uint64
	intc 0 // 0
	frame_dig 6 // i: uint64
	divmodw
	pop
	pop
	swap
	!

	// wideRatio failed
	assert
	frame_bury 7 // delta: uint64

	// contracts/BalancedPoolV2.algo.ts:381
	// result = neg ? result - delta : result + delta
	frame_dig 5 // neg: bool
	bz *ternary0_false
	frame_dig 3 // result: uint64
	frame_dig 7 // delta: uint64
	-
	b *ternary0_end

*ternary0_false:
	frame_dig 3 // result: uint64
	frame_dig 7 // delta: uint64
	+

*ternary0_end:
	frame_bury 3 // result: uint64

	// contracts/BalancedPoolV2.algo.ts:382
	// neg = !neg
	frame_dig 5 // neg: bool
	!
	frame_bury 5 // neg: bool

*for_5_continue:
	// contracts/BalancedPoolV2.algo.ts:378
	// i = i + 1
	frame_dig 6 // i: uint64
	intc 2 // 1
	+
	frame_bury 6 // i: uint64
	b *for_5

*for_5_end:
	// contracts/BalancedPoolV2.algo.ts:385
	// return [negative, result];
	frame_dig 0 // negative: uint64
	itob
	frame_dig 3 // result: uint64
	itob
	concat

	// set the subroutine return value
	frame_bury 0

	// pop all local variables from the stack
	popn 7
	retsub

// exp(x: uint64): uint64
//
// Approximates the exponential function e^x for a fixed-point input.
//
// Uses a truncated Taylor series expansion of e^x:
// e^x ≈ 1 + x + x^2/2! + x^3/3! + ... + x^n/n!
//
// @param x - Exponent in fixed-point representation (scaled by SCALE).
// @returns Approximated e^x value in fixed-point representation.
exp:
	proto 1 1

	// Push empty bytes after the frame pointer to reserve space for local variables
	bytec 0 // 0x
	dupn 2

	// contracts/BalancedPoolV2.algo.ts:398
	// result = SCALE
	intc 3 // 1000000
	frame_bury 0 // result: uint64

	// contracts/BalancedPoolV2.algo.ts:399
	// term = SCALE
	intc 3 // 1000000
	frame_bury 1 // term: uint64

	// contracts/BalancedPoolV2.algo.ts:401
	// for (let i = 1; i <= 10; i = i + 1)
	intc 2 // 1
	frame_bury 2 // i: uint64

*for_6:
	// contracts/BalancedPoolV2.algo.ts:401
	// i <= 10
	frame_dig 2 // i: uint64
	intc 4 // 10
	<=
	bz *for_6_end

	// contracts/BalancedPoolV2.algo.ts:402
	// term = wideRatio([term, x], [i * SCALE])
	frame_dig 1 // term: uint64
	frame_dig -1 // x: uint64
	mulw
	intc 0 // 0
	frame_dig 2 // i: uint64
	intc 3 // 1000000
	*
	divmodw
	pop
	pop
	swap
	!

	// wideRatio failed
	assert
	frame_bury 1 // term: uint64

	// contracts/BalancedPoolV2.algo.ts:403
	// result += term
	frame_dig 0 // result: uint64
	frame_dig 1 // term: uint64
	+
	frame_bury 0 // result: uint64

*for_6_continue:
	// contracts/BalancedPoolV2.algo.ts:401
	// i = i + 1
	frame_dig 2 // i: uint64
	intc 2 // 1
	+
	frame_bury 2 // i: uint64
	b *for_6

*for_6_end:
	// contracts/BalancedPoolV2.algo.ts:406
	// return result;
	frame_dig 0 // result: uint64

	// set the subroutine return value
	frame_bury 0

	// pop all local variables from the stack
	popn 2
	retsub

// pow(x: uint64, y: uint64): uint64
//
// Approximates x rise to the power of y (i.e., x^y) in fixed-point arithmetic.
//
// Internally implemented using:
// x^y = exp(y * ln(x))
// Handles x < 1 via sign-aware logarithm and inversion logic.
//
// @param x - Base value in fixed-point representation.
// @param y - Exponent in fixed-point representation.
// @returns Approximated result of x^y in fixed-point.
pow:
	proto 2 1

	// Push empty bytes after the frame pointer to reserve space for local variables
	bytec 0 // 0x
	dupn 4

	// *if8_condition
	// contracts/BalancedPoolV2.algo.ts:421
	// x === 0
	frame_dig -1 // x: uint64
	intc 0 // 0
	==
	bz *if8_end

	// *if8_consequent
	// contracts/BalancedPoolV2.algo.ts:421
	// return 0;
	intc 0 // 0
	b *pow*return

*if8_end:
	// contracts/BalancedPoolV2.algo.ts:423
	// lnXResult = this.lnWithSign(x)
	frame_dig -1 // x: uint64
	callsub lnWithSign
	frame_bury 0 // lnXResult: uint64[]

	// contracts/BalancedPoolV2.algo.ts:424
	// negativeLn = lnXResult[0]
	frame_dig 0 // lnXResult: uint64[]
	store 255 // full array
	load 255 // full array
	extract 0 8
	btoi
	frame_bury 1 // negativeLn: uint64

	// contracts/BalancedPoolV2.algo.ts:425
	// lnX = lnXResult[1]
	frame_dig 0 // lnXResult: uint64[]
	store 255 // full array
	load 255 // full array
	extract 8 8
	btoi
	frame_bury 2 // lnX: uint64

	// contracts/BalancedPoolV2.algo.ts:427
	// ylnX = wideRatio([y, lnX], [SCALE])
	frame_dig -2 // y: uint64
	frame_dig 2 // lnX: uint64
	mulw
	intc 0 // 0
	intc 3 // 1000000
	divmodw
	pop
	pop
	swap
	!

	// wideRatio failed
	assert
	frame_bury 3 // ylnX: uint64

	// contracts/BalancedPoolV2.algo.ts:429
	// expResult = this.exp(ylnX)
	frame_dig 3 // ylnX: uint64
	callsub exp
	frame_bury 4 // expResult: uint64

	// *if9_condition
	// contracts/BalancedPoolV2.algo.ts:431
	// negativeLn === 1
	frame_dig 1 // negativeLn: uint64
	intc 2 // 1
	==
	bz *if9_end

	// *if9_consequent
	// contracts/BalancedPoolV2.algo.ts:432
	// return wideRatio([SCALE, SCALE], [expResult]);
	intc 3 // 1000000
	dup
	mulw
	intc 0 // 0
	frame_dig 4 // expResult: uint64
	divmodw
	pop
	pop
	swap
	!

	// wideRatio failed
	assert
	b *pow*return

*if9_end:
	// contracts/BalancedPoolV2.algo.ts:435
	// return expResult;
	frame_dig 4 // expResult: uint64

*pow*return:
	// set the subroutine return value
	frame_bury 0

	// pop all local variables from the stack
	popn 4
	retsub

// calcOut(balanceIn: uint64, weightIn: uint64, balanceOut: uint64, weightOut: uint64, amountIn: uint64): uint64
//
// Calculates the output amount of a token swap using the constant mean formula
// with weight-based pricing and an optional fee.
//
// The formula used is derived from the Balancer-style AMM:
//
// amountOut = balanceOut * (1 - (balanceIn / (balanceIn + amountInWithFee))^(weightIn / weightOut))
//
// This ensures price sensitivity based on both token weights and pool balances.
// A swap fee is applied by reducing the effective input amount.
//
// @param balanceIn - Current balance of the input asset in the pool.
// @param weightIn - Weight of the input asset, scaled by SCALE.
// @param balanceOut - Current balance of the output asset in the pool.
// @param weightOut - Weight of the output asset, scaled by SCALE.
// @param amountIn - Amount of input asset sent by the user.
// @returns The amount of output asset the user will receive.
calcOut:
	proto 5 1

	// Push empty bytes after the frame pointer to reserve space for local variables
	bytec 0 // 0x
	dupn 3

	// contracts/BalancedPoolV2.algo.ts:465
	// amountInWithFee = wideRatio([amountIn, SCALE - fee], [SCALE])
	frame_dig -5 // amountIn: uint64
	pushint 999000
	mulw
	intc 0 // 0
	intc 3 // 1000000
	divmodw
	pop
	pop
	swap
	!

	// wideRatio failed
	assert
	frame_bury 0 // amountInWithFee: uint64

	// contracts/BalancedPoolV2.algo.ts:468
	// ratio = wideRatio([balanceIn, SCALE], [balanceIn + amountInWithFee])
	frame_dig -1 // balanceIn: uint64
	intc 3 // 1000000
	mulw
	intc 0 // 0
	frame_dig -1 // balanceIn: uint64
	frame_dig 0 // amountInWithFee: uint64
	+
	divmodw
	pop
	pop
	swap
	!

	// wideRatio failed
	assert
	frame_bury 1 // ratio: uint64

	// contracts/BalancedPoolV2.algo.ts:470
	// power = wideRatio([weightIn, SCALE], [weightOut])
	frame_dig -2 // weightIn: uint64
	intc 3 // 1000000
	mulw
	intc 0 // 0
	frame_dig -4 // weightOut: uint64
	divmodw
	pop
	pop
	swap
	!

	// wideRatio failed
	assert
	frame_bury 2 // power: uint64

	// contracts/BalancedPoolV2.algo.ts:473
	// ratioPow = this.pow(ratio, power)
	frame_dig 2 // power: uint64
	frame_dig 1 // ratio: uint64
	callsub pow
	frame_bury 3 // ratioPow: uint64

	// contracts/BalancedPoolV2.algo.ts:475
	// log(itob(balanceIn))
	frame_dig -1 // balanceIn: uint64
	itob
	log

	// contracts/BalancedPoolV2.algo.ts:476
	// log(itob(amountInWithFee))
	frame_dig 0 // amountInWithFee: uint64
	itob
	log

	// contracts/BalancedPoolV2.algo.ts:477
	// log(itob(ratio))
	frame_dig 1 // ratio: uint64
	itob
	log

	// contracts/BalancedPoolV2.algo.ts:478
	// log(itob(power))
	frame_dig 2 // power: uint64
	itob
	log

	// contracts/BalancedPoolV2.algo.ts:479
	// log(itob(ratioPow))
	frame_dig 3 // ratioPow: uint64
	itob
	log

	// contracts/BalancedPoolV2.algo.ts:480
	// log(itob(wideRatio([balanceOut, SCALE - ratioPow], [SCALE])))
	frame_dig -3 // balanceOut: uint64
	intc 3 // 1000000
	frame_dig 3 // ratioPow: uint64
	-
	mulw
	intc 0 // 0
	intc 3 // 1000000
	divmodw
	pop
	pop
	swap
	!

	// wideRatio failed
	assert
	itob
	log

	// contracts/BalancedPoolV2.algo.ts:482
	// return wideRatio([balanceOut, SCALE - ratioPow], [SCALE]);
	frame_dig -3 // balanceOut: uint64
	intc 3 // 1000000
	frame_dig 3 // ratioPow: uint64
	-
	mulw
	intc 0 // 0
	intc 3 // 1000000
	divmodw
	pop
	pop
	swap
	!

	// wideRatio failed
	assert

	// set the subroutine return value
	frame_bury 0

	// pop all local variables from the stack
	popn 3
	retsub

// computeNAssetsLiquidity(sender: Address): uint64
//
// Computes the amount of LP tokens to mint for a user based on the assets they provided,
// using the constant mean formula with weight sensitivity.
//
// This method calculates the geometric mean of each provided asset relative to the pool's balance,
// adjusted by its weight. The formula is:
//
// liquidity = totalLP * Π_i (provided_i / (balance_i - provided_i)) ^ weight_i
//
// It ensures proportional liquidity provisioning across all assets, weighted appropriately.
// The liquidity amount is scaled by the product of powered ratios and the total LP supply.
//
// During execution, this function also resets the sender's `provided` vector to zero,
// consuming the state used to compute the liquidity. This ensures the same input
// cannot be reused in future calculations.
//
// @param sender - The user address for which liquidity is being computed.
// @returns The amount of LP tokens to mint for the sender.
computeNAssetsLiquidity:
	proto 1 1

	// Push empty bytes after the frame pointer to reserve space for local variables
	bytec 0 // 0x
	dupn 10

	// contracts/BalancedPoolV2.algo.ts:505
	// totalAssets = this.assets.value.length
	bytec 2 //  "assets"
	app_global_get
	extract 2 0
	len
	intc 1 // 8
	/
	frame_bury 0 // totalAssets: uint64

	// contracts/BalancedPoolV2.algo.ts:506
	// assert(totalAssets >= 1, 'Please provide at least one asset')
	frame_dig 0 // totalAssets: uint64
	intc 2 // 1
	>=

	// Please provide at least one asset
	assert

	// contracts/BalancedPoolV2.algo.ts:508
	// ratio = SCALE
	intc 3 // 1000000
	frame_bury 1 // ratio: uint64

	// contracts/BalancedPoolV2.algo.ts:510
	// for (let i = 0; i < totalAssets - 1; i += 1)
	intc 0 // 0
	frame_bury 2 // i: uint64

*for_7:
	// contracts/BalancedPoolV2.algo.ts:510
	// i < totalAssets - 1
	frame_dig 2 // i: uint64
	frame_dig 0 // totalAssets: uint64
	intc 2 // 1
	-
	<
	bz *for_7_end

	// contracts/BalancedPoolV2.algo.ts:511
	// increaseOpcodeBudget()
	itxn_begin
	intc 5 //  appl
	itxn_field TypeEnum
	intc 0 // 0
	itxn_field Fee
	bytec 11 //  #pragma version 10; int 1
	dup
	itxn_field ApprovalProgram
	itxn_field ClearStateProgram
	intc 9 //  DeleteApplication
	itxn_field OnCompletion
	itxn_submit

*for_7_continue:
	// contracts/BalancedPoolV2.algo.ts:510
	// i += 1
	frame_dig 2 // i: uint64
	intc 2 // 1
	+
	frame_bury 2 // i: uint64
	b *for_7

*for_7_end:
	// contracts/BalancedPoolV2.algo.ts:514
	// for (let i = 0; i < totalAssets; i += 1)
	intc 0 // 0
	frame_bury 3 // i: uint64

*for_8:
	// contracts/BalancedPoolV2.algo.ts:514
	// i < totalAssets
	frame_dig 3 // i: uint64
	frame_dig 0 // totalAssets: uint64
	<
	bz *for_8_end

	// contracts/BalancedPoolV2.algo.ts:515
	// assetId = this.assets.value[i]
	bytec 2 //  "assets"
	app_global_get
	extract 2 0
	store 255 // full array
	intc 0 //  initial offset
	frame_dig 3 // i: uint64
	intc 1 // 8
	* // acc * typeLength
	+
	load 255 // full array
	swap
	intc 1 // 8
	extract3
	btoi
	frame_bury 4 // assetId: uint64

	// contracts/BalancedPoolV2.algo.ts:516
	// poolBalance = this.balances(assetId).value
	bytec 1 //  "balances_"
	frame_dig 4 // assetId: uint64
	itob
	concat
	box_get

	// box value does not exist: this.balances(assetId).value
	assert
	btoi
	frame_bury 5 // poolBalance: uint64

	// contracts/BalancedPoolV2.algo.ts:517
	// providedAmount = this.provided(sender).value[i]
	bytec 3 //  "provided_"
	frame_dig -1 // sender: Address
	concat
	box_get

	// box value does not exist: this.provided(sender).value
	assert
	extract 2 0
	store 255 // full array
	intc 0 //  initial offset
	frame_dig 3 // i: uint64
	intc 1 // 8
	* // acc * typeLength
	+
	load 255 // full array
	swap
	intc 1 // 8
	extract3
	btoi
	frame_bury 6 // providedAmount: uint64

	// contracts/BalancedPoolV2.algo.ts:518
	// weight = this.getCurrentWeight(i)
	frame_dig 3 // i: uint64
	callsub getCurrentWeight
	frame_bury 7 // weight: uint64

	// contracts/BalancedPoolV2.algo.ts:520
	// assert(poolBalance > 0, 'Pool balance must be > 0')
	frame_dig 5 // poolBalance: uint64
	intc 0 // 0
	>

	// Pool balance must be > 0
	assert

	// contracts/BalancedPoolV2.algo.ts:522
	// assetRatio = wideRatio([providedAmount, SCALE], [poolBalance - providedAmount])
	frame_dig 6 // providedAmount: uint64
	intc 3 // 1000000
	mulw
	intc 0 // 0
	frame_dig 5 // poolBalance: uint64
	frame_dig 6 // providedAmount: uint64
	-
	divmodw
	pop
	pop
	swap
	!

	// wideRatio failed
	assert
	frame_bury 8 // assetRatio: uint64

	// contracts/BalancedPoolV2.algo.ts:523
	// powed = this.pow(assetRatio, weight)
	frame_dig 7 // weight: uint64
	frame_dig 8 // assetRatio: uint64
	callsub pow
	frame_bury 9 // powed: uint64

	// contracts/BalancedPoolV2.algo.ts:524
	// ratio = wideRatio([ratio, powed], [SCALE])
	frame_dig 1 // ratio: uint64
	frame_dig 9 // powed: uint64
	mulw
	intc 0 // 0
	intc 3 // 1000000
	divmodw
	pop
	pop
	swap
	!

	// wideRatio failed
	assert
	frame_bury 1 // ratio: uint64

	// contracts/BalancedPoolV2.algo.ts:526
	// this.provided(sender).value[i] = 0
	bytec 3 //  "provided_"
	frame_dig -1 // sender: Address
	concat
	box_get

	// box value does not exist: this.provided(sender).value
	assert
	extract 2 0
	store 255 // full array
	intc 0 //  initial offset
	frame_dig 3 // i: uint64
	intc 1 // 8
	* // acc * typeLength
	+
	load 255 // full array
	swap
	bytec 12 // 0x0000000000000000
	replace3
	bytec 3 //  "provided_"
	frame_dig -1 // sender: Address
	concat
	dup
	box_del
	pop
	swap
	dup
	len
	intc 1 // 8
	/
	itob
	extract 6 2
	swap
	concat
	box_put

*for_8_continue:
	// contracts/BalancedPoolV2.algo.ts:514
	// i += 1
	frame_dig 3 // i: uint64
	intc 2 // 1
	+
	frame_bury 3 // i: uint64
	b *for_8

*for_8_end:
	// contracts/BalancedPoolV2.algo.ts:529
	// totalLP = this.totalLP()
	callsub totalLP
	frame_bury 10 // totalLP: uint64

	// contracts/BalancedPoolV2.algo.ts:530
	// return wideRatio([totalLP, ratio], [SCALE]);
	frame_dig 10 // totalLP: uint64
	frame_dig 1 // ratio: uint64
	mulw
	intc 0 // 0
	intc 3 // 1000000
	divmodw
	pop
	pop
	swap
	!

	// wideRatio failed
	assert

	// set the subroutine return value
	frame_bury 0

	// pop all local variables from the stack
	popn 10
	retsub

// totalLP(): uint64
//
// Returns the total circulating supply of LP tokens in the pool.
//
// Circulating LP supply is calculated as:
// totalIssued - reserveBalance - burned
//
// - `totalIssued`: the total supply originally created by the pool.
// - `reserveBalance`: LP tokens still held in the reserve (i.e., the pool itself).
// - `burned`: total LP tokens permanently removed via `burnLiquidity()`.
//
// This value is used in proportional calculations such as minting or burning LP tokens.
//
// @returns The current total circulating LP token supply.
totalLP:
	proto 0 1

	// contracts/BalancedPoolV2.algo.ts:548
	// return this.token.value.total - this.token.value.reserve.assetBalance(this.token.value) - this.burned.value;
	bytec 5 //  "token"
	app_global_get
	asset_params_get AssetTotal
	pop
	bytec 5 //  "token"
	app_global_get
	asset_params_get AssetReserve
	pop
	bytec 5 //  "token"
	app_global_get
	asset_holding_get AssetBalance
	pop
	-
	bytec 10 //  "burned"
	app_global_get
	-
	retsub

// absDiff(a: uint64, b: uint64): uint64
//
// Returns the absolute difference between two unsigned integers.
//
// Equivalent to:
// |a - b| = (a > b) ? a - b : b - a
//
// Useful in cases where the ordering of values is uncertain, but the magnitude
// of their difference is important (e.g., weight normalization tolerances).
//
// @param a - First value.
// @param b - Second value.
// @returns The absolute difference between `a` and `b`.
absDiff:
	proto 2 1

	// contracts/BalancedPoolV2.algo.ts:565
	// return a > b ? a - b : b - a;
	frame_dig -1 // a: uint64
	frame_dig -2 // b: uint64
	>
	bz *ternary1_false
	frame_dig -1 // a: uint64
	frame_dig -2 // b: uint64
	-
	b *ternary1_end

*ternary1_false:
	frame_dig -2 // b: uint64
	frame_dig -1 // a: uint64
	-

*ternary1_end:
	retsub

// getTotalAssets()uint64
*abi_route_getTotalAssets:
	// The ABI return prefix
	bytec 4 // 0x151f7c75

	// execute getTotalAssets()uint64
	callsub getTotalAssets
	itob
	concat
	log
	intc 2 // 1
	return

// getTotalAssets(): uint64
getTotalAssets:
	proto 0 1

	// contracts/BalancedPoolV2.algo.ts:570
	// return this.assets.value.length;
	bytec 2 //  "assets"
	app_global_get
	extract 2 0
	len
	intc 1 // 8
	/
	retsub

// getToken()uint64
*abi_route_getToken:
	// The ABI return prefix
	bytec 4 // 0x151f7c75

	// execute getToken()uint64
	callsub getToken
	itob
	concat
	log
	intc 2 // 1
	return

// getToken(): AssetID
getToken:
	proto 0 1

	// contracts/BalancedPoolV2.algo.ts:575
	// return this.token.value;
	bytec 5 //  "token"
	app_global_get
	retsub

// getBalance(uint64)uint64
*abi_route_getBalance:
	// The ABI return prefix
	bytec 4 // 0x151f7c75

	// index: uint64
	txna ApplicationArgs 1
	btoi

	// execute getBalance(uint64)uint64
	callsub getBalance
	itob
	concat
	log
	intc 2 // 1
	return

// getBalance(index: uint64): uint64
getBalance:
	proto 1 1

	// Push empty bytes after the frame pointer to reserve space for local variables
	bytec 0 // 0x

	// contracts/BalancedPoolV2.algo.ts:580
	// asset = this.assets.value[index]
	bytec 2 //  "assets"
	app_global_get
	extract 2 0
	store 255 // full array
	intc 0 //  initial offset
	frame_dig -1 // index: uint64
	intc 1 // 8
	* // acc * typeLength
	+
	load 255 // full array
	swap
	intc 1 // 8
	extract3
	btoi
	frame_bury 0 // asset: uint64

	// contracts/BalancedPoolV2.algo.ts:581
	// return this.balances(asset).value;
	bytec 1 //  "balances_"
	frame_dig 0 // asset: uint64
	itob
	concat
	box_get

	// box value does not exist: this.balances(asset).value
	assert
	btoi

	// set the subroutine return value
	frame_bury 0
	retsub

// estimateSwap(uint64,uint64,uint64)uint64
*abi_route_estimateSwap:
	// The ABI return prefix
	bytec 4 // 0x151f7c75

	// amount: uint64
	txna ApplicationArgs 3
	btoi

	// to: uint64
	txna ApplicationArgs 2
	btoi

	// from: uint64
	txna ApplicationArgs 1
	btoi

	// execute estimateSwap(uint64,uint64,uint64)uint64
	callsub estimateSwap
	itob
	concat
	log
	intc 2 // 1
	return

// estimateSwap(from: uint64, to: uint64, amount: uint64): uint64
estimateSwap:
	proto 3 1

	// Push empty bytes after the frame pointer to reserve space for local variables
	bytec 0 // 0x
	dupn 5

	// contracts/BalancedPoolV2.algo.ts:586
	// assetIn = this.assets.value[from]
	bytec 2 //  "assets"
	app_global_get
	extract 2 0
	store 255 // full array
	intc 0 //  initial offset
	frame_dig -1 // from: uint64
	intc 1 // 8
	* // acc * typeLength
	+
	load 255 // full array
	swap
	intc 1 // 8
	extract3
	btoi
	frame_bury 0 // assetIn: uint64

	// contracts/BalancedPoolV2.algo.ts:587
	// assetOut = this.assets.value[to]
	bytec 2 //  "assets"
	app_global_get
	extract 2 0
	store 255 // full array
	intc 0 //  initial offset
	frame_dig -2 // to: uint64
	intc 1 // 8
	* // acc * typeLength
	+
	load 255 // full array
	swap
	intc 1 // 8
	extract3
	btoi
	frame_bury 1 // assetOut: uint64

	// contracts/BalancedPoolV2.algo.ts:589
	// balanceIn = this.balances(assetIn).value
	bytec 1 //  "balances_"
	frame_dig 0 // assetIn: uint64
	itob
	concat
	box_get

	// box value does not exist: this.balances(assetIn).value
	assert
	btoi
	frame_bury 2 // balanceIn: uint64

	// contracts/BalancedPoolV2.algo.ts:590
	// balanceOut = this.balances(assetOut).value
	bytec 1 //  "balances_"
	frame_dig 1 // assetOut: uint64
	itob
	concat
	box_get

	// box value does not exist: this.balances(assetOut).value
	assert
	btoi
	frame_bury 3 // balanceOut: uint64

	// contracts/BalancedPoolV2.algo.ts:592
	// weightIn = this.getCurrentWeight(from)
	frame_dig -1 // from: uint64
	callsub getCurrentWeight
	frame_bury 4 // weightIn: uint64

	// contracts/BalancedPoolV2.algo.ts:593
	// weightOut = this.getCurrentWeight(to)
	frame_dig -2 // to: uint64
	callsub getCurrentWeight
	frame_bury 5 // weightOut: uint64

	// contracts/BalancedPoolV2.algo.ts:595
	// return this.calcOut(balanceIn, weightIn, balanceOut, weightOut, amount);
	frame_dig -3 // amount: uint64
	frame_dig 5 // weightOut: uint64
	frame_dig 3 // balanceOut: uint64
	frame_dig 4 // weightIn: uint64
	frame_dig 2 // balanceIn: uint64
	callsub calcOut

	// set the subroutine return value
	frame_bury 0

	// pop all local variables from the stack
	popn 5
	retsub

// getCurrentWeight(uint64)uint64
*abi_route_getCurrentWeight:
	// The ABI return prefix
	bytec 4 // 0x151f7c75

	// index: uint64
	txna ApplicationArgs 1
	btoi

	// execute getCurrentWeight(uint64)uint64
	callsub getCurrentWeight
	itob
	concat
	log
	intc 2 // 1
	return

// getCurrentWeight(index: uint64): uint64
getCurrentWeight:
	proto 1 1

	// Push empty bytes after the frame pointer to reserve space for local variables
	bytec 0 // 0x
	dupn 8

	// contracts/BalancedPoolV2.algo.ts:600
	// now = globals.latestTimestamp
	global LatestTimestamp
	frame_bury 0 // now: uint64

	// contracts/BalancedPoolV2.algo.ts:601
	// start = this.startTime.value
	bytec 8 //  "start_time"
	app_global_get
	frame_bury 1 // start: uint64

	// contracts/BalancedPoolV2.algo.ts:602
	// end = this.endTime.value
	bytec 6 //  "end_time"
	app_global_get
	frame_bury 2 // end: uint64

	// *if10_condition
	// contracts/BalancedPoolV2.algo.ts:604
	// now <= start || start === 0 || end === 0
	frame_dig 0 // now: uint64
	frame_dig 1 // start: uint64
	<=
	dup
	bnz *skip_or0
	frame_dig 1 // start: uint64
	intc 0 // 0
	==
	||

*skip_or0:
	dup
	bnz *skip_or1
	frame_dig 2 // end: uint64
	intc 0 // 0
	==
	||

*skip_or1:
	bz *if10_end

	// *if10_consequent
	// contracts/BalancedPoolV2.algo.ts:605
	// return this.weights(index).value;
	bytec 7 //  "weights_"
	frame_dig -1 // index: uint64
	itob
	concat
	box_get

	// box value does not exist: this.weights(index).value
	assert
	btoi
	b *getCurrentWeight*return

*if10_end:
	// *if11_condition
	// contracts/BalancedPoolV2.algo.ts:608
	// now >= end
	frame_dig 0 // now: uint64
	frame_dig 2 // end: uint64
	>=
	bz *if11_end

	// *if11_consequent
	// contracts/BalancedPoolV2.algo.ts:609
	// return this.targetWeights(index).value;
	bytec 9 //  "target_weights_"
	frame_dig -1 // index: uint64
	itob
	concat
	box_get

	// box value does not exist: this.targetWeights(index).value
	assert
	btoi
	b *getCurrentWeight*return

*if11_end:
	// contracts/BalancedPoolV2.algo.ts:612
	// elapsed = now - start
	frame_dig 0 // now: uint64
	frame_dig 1 // start: uint64
	-
	frame_bury 3 // elapsed: uint64

	// contracts/BalancedPoolV2.algo.ts:613
	// total = end - start
	frame_dig 2 // end: uint64
	frame_dig 1 // start: uint64
	-
	frame_bury 4 // total: uint64

	// contracts/BalancedPoolV2.algo.ts:615
	// w0 = this.weights(index).value
	bytec 7 //  "weights_"
	frame_dig -1 // index: uint64
	itob
	concat
	box_get

	// box value does not exist: this.weights(index).value
	assert
	btoi
	frame_bury 5 // w0: uint64

	// contracts/BalancedPoolV2.algo.ts:616
	// w1 = this.targetWeights(index).value
	bytec 9 //  "target_weights_"
	frame_dig -1 // index: uint64
	itob
	concat
	box_get

	// box value does not exist: this.targetWeights(index).value
	assert
	btoi
	frame_bury 6 // w1: uint64

	// contracts/BalancedPoolV2.algo.ts:618
	// delta = w1 > w0 ? w1 - w0 : w0 - w1
	frame_dig 6 // w1: uint64
	frame_dig 5 // w0: uint64
	>
	bz *ternary2_false
	frame_dig 6 // w1: uint64
	frame_dig 5 // w0: uint64
	-
	b *ternary2_end

*ternary2_false:
	frame_dig 5 // w0: uint64
	frame_dig 6 // w1: uint64
	-

*ternary2_end:
	frame_bury 7 // delta: uint64

	// contracts/BalancedPoolV2.algo.ts:619
	// offset = wideRatio([delta, elapsed], [total])
	frame_dig 7 // delta: uint64
	frame_dig 3 // elapsed: uint64
	mulw
	intc 0 // 0
	frame_dig 4 // total: uint64
	divmodw
	pop
	pop
	swap
	!

	// wideRatio failed
	assert
	frame_bury 8 // offset: uint64

	// contracts/BalancedPoolV2.algo.ts:621
	// return w1 > w0 ? w0 + offset : w0 - offset;
	frame_dig 6 // w1: uint64
	frame_dig 5 // w0: uint64
	>
	bz *ternary3_false
	frame_dig 5 // w0: uint64
	frame_dig 8 // offset: uint64
	+
	b *ternary3_end

*ternary3_false:
	frame_dig 5 // w0: uint64
	frame_dig 8 // offset: uint64
	-

*ternary3_end:

*getCurrentWeight*return:
	// set the subroutine return value
	frame_bury 0

	// pop all local variables from the stack
	popn 8
	retsub

// getTimes()uint64[]
*abi_route_getTimes:
	// The ABI return prefix
	bytec 4 // 0x151f7c75

	// execute getTimes()uint64[]
	callsub getTimes
	dup
	len
	intc 1 // 8
	/
	itob
	extract 6 2
	swap
	concat
	concat
	log
	intc 2 // 1
	return

// getTimes(): uint64[]
getTimes:
	proto 0 1

	// contracts/BalancedPoolV2.algo.ts:626
	// return [this.startTime.value, this.endTime.value, globals.latestTimestamp];
	bytec 8 //  "start_time"
	app_global_get
	itob
	bytec 6 //  "end_time"
	app_global_get
	itob
	concat
	global LatestTimestamp
	itob
	concat
	retsub

*create_NoOp:
	txn NumAppArgs
	bz *abi_route_createApplication
	pushbytes 0xb8447b36 // method "createApplication()void"
	txna ApplicationArgs 0
	match *abi_route_createApplication

	// this contract does not implement the given ABI method for create NoOp
	err

*call_NoOp:
	pushbytes 0xe635fdb5 // method "bootstrap(uint64[],uint64[])uint64"
	pushbytes 0xabdf7e15 // method "addLiquidity(uint64,uint64,address)void"
	pushbytes 0x2a74004f // method "getLiquidity(address)uint64"
	pushbytes 0x3fb1d09b // method "burnLiquidity(address,uint64)void"
	pushbytes 0x8d325e29 // method "swap(address,uint64,uint64,uint64)uint64"
	pushbytes 0xf47ba5ea // method "changeWeights(uint64,uint64[])uint64"
	pushbytes 0x7194259e // method "getTotalAssets()uint64"
	pushbytes 0x20eb5394 // method "getToken()uint64"
	pushbytes 0x41310aee // method "getBalance(uint64)uint64"
	pushbytes 0x29200583 // method "estimateSwap(uint64,uint64,uint64)uint64"
	pushbytes 0x287889e2 // method "getCurrentWeight(uint64)uint64"
	pushbytes 0x89915be2 // method "getTimes()uint64[]"
	txna ApplicationArgs 0
	match *abi_route_bootstrap *abi_route_addLiquidity *abi_route_getLiquidity *abi_route_burnLiquidity *abi_route_swap *abi_route_changeWeights *abi_route_getTotalAssets *abi_route_getToken *abi_route_getBalance *abi_route_estimateSwap *abi_route_getCurrentWeight *abi_route_getTimes

	// this contract does not implement the given ABI method for call NoOp
	err

*intToAscii:
	proto 1 1
	pushbytes 0x30313233343536373839 // "0123456789"
	frame_dig -1 // i: uint64
	intc 2 // 1
	extract3
	retsub
	

*itoa:
	proto 1 1
	frame_dig -1 // i: uint64
	intc 0 // 0
	==
	bz *itoa_if_end
	pushbytes 0x30
	retsub

*itoa_if_end:
	frame_dig -1 // i: uint64
	intc 4 // 10
	/
	intc 0 // 0
	>
	bz *itoa_ternary_false
	frame_dig -1 // i: uint64
	intc 4 // 10
	/
	callsub *itoa
	b *itoa_ternary_end

*itoa_ternary_false:
	bytec 0 //  ""

*itoa_ternary_end:
	frame_dig -1 // i: uint64
	intc 4 // 10
	%
	callsub *intToAscii
	concat
	retsub